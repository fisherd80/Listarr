services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      - FLASK_DEBUG=false
      - LISTARR_SECRET_KEY=dev_key_change_me
    restart: unless-stopped
