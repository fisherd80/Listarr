# Plan 10.1-01 Summary: JS Consistency Fixes

**Phase:** 10.1 - UI Review Fixes
**Plan:** 01 of 3
**Status:** Complete
**Date:** 2026-02-08

## What Was Done

### Task 1: Replace alert() with showToast() and add generateServiceBadge()
- Replaced 4 `alert()` calls in config.js with `showToast()` using "warning" type
- Added `generateServiceBadge(service)` function to utils.js (Radarr=amber, Sonarr=blue)
- Badge colors match server-side `service_badge()` Jinja2 macro in macros/status.html
- **Commit:** `84da90e`

### Task 2: Update dashboard.js badges and fix jobs.html dropdown heights
- Replaced hardcoded `serviceColor` variable in `updateUpcomingWidget()` with `generateServiceBadge()`
- Removed wrong colors: Radarr was blue (should be amber), Sonarr was purple (should be blue)
- Added `py-2` class to both filter `<select>` elements in jobs.html for consistent dropdown height
- **Commit:** `e51ec1c`

## Files Modified

| File | Change |
|------|--------|
| listarr/static/js/config.js | 4 alert() -> showToast() |
| listarr/static/js/utils.js | Added generateServiceBadge() function |
| listarr/static/js/dashboard.js | Replaced hardcoded badge colors with generateServiceBadge() |
| listarr/templates/jobs.html | Added py-2 to filter select elements |

## Verification

- Zero `alert()` calls in config.js
- `generateServiceBadge()` in utils.js with correct amber/blue colors
- Dashboard upcoming widget uses `generateServiceBadge()` (no hardcoded colors)
- Zero `bg-purple` references in dashboard.js
- Both jobs.html filter selects include `py-2`
- All 453 tests pass (no modifications needed)

## Deviations

None.

## Issues Discovered

None.
