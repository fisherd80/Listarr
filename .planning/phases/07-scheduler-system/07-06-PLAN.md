---
phase: 07-scheduler-system
plan: 06
type: execute
wave: 5
depends_on: ["07-04", "07-05"]
files_modified:
  - README.md
  - CHANGELOG.md
  - CLAUDE.md
autonomous: false

must_haves:
  truths:
    - "README documents scheduler feature and configuration"
    - "CHANGELOG documents Phase 7 changes"
    - "CLAUDE.md updated with scheduler patterns"
  artifacts:
    - path: "README.md"
      provides: "Updated documentation"
      contains: "schedule"
    - path: "CHANGELOG.md"
      provides: "Phase 7 changelog entry"
      contains: "Scheduler"
  key_links: []
---

<objective>
Update project documentation to reflect the new scheduler functionality.

Purpose: Keep documentation current per established standards (README.md, CHANGELOG.md, CLAUDE.md).
Output: Updated documentation files reflecting Phase 7 features.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scheduler-system/07-CONTEXT.md
@README.md
@CHANGELOG.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with scheduler documentation</name>
  <files>README.md</files>
  <action>
Update README.md to document the scheduler feature:

1. **Features section:** Add "Automated Scheduling" to the feature list
   - Cron-based scheduling for automatic list execution
   - Presets for common intervals (hourly, daily, weekly)
   - Custom cron expressions for advanced users
   - Global pause toggle for maintenance

2. **Configuration section:** Add scheduler-related environment variables
   - TZ: Server timezone for schedule interpretation (default: UTC)
   - SCHEDULER_WORKER: Internal Gunicorn worker flag (auto-managed)

3. **Usage section:** Brief explanation of how to schedule lists
   - Create list via wizard with schedule preset
   - Edit existing lists to add/change schedules
   - Use Schedule page for overview and global controls
   - View upcoming jobs on Dashboard

Keep documentation concise and user-focused. Reference the Schedule page as the management interface.
  </action>
  <verify>
Review README.md to confirm:
1. Scheduler feature is documented
2. TZ environment variable is mentioned
3. Usage instructions are clear
  </verify>
  <done>
README.md documents scheduler feature, configuration, and usage
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md with Phase 7 entry</name>
  <files>CHANGELOG.md</files>
  <action>
Add Phase 7 entry to CHANGELOG.md following the existing format:

```markdown
## [Phase 7] - Scheduler System - YYYY-MM-DD

### Added
- Cron-based job scheduling with APScheduler
- Schedule page for managing scheduled lists
- Global pause toggle to temporarily stop all scheduled jobs
- Upcoming jobs widget on Dashboard
- Next run time display on Lists page
- Schedule presets: hourly, every 6h, daily, weekly
- Custom cron expression support
- Human-readable cron descriptions

### Changed
- ServiceConfig model now includes scheduler_paused flag
- Gunicorn config identifies first worker for scheduler
- Edit form syncs schedule changes with scheduler

### Technical
- APScheduler 3.11.2 for job scheduling
- cronsim 2.7 for cron validation
- cron-descriptor 2.0.5 for human-readable text
- Single-worker scheduler pattern to prevent duplicate execution
```

Adjust date to actual completion date.
  </action>
  <verify>
Review CHANGELOG.md to confirm:
1. Phase 7 entry exists with correct format
2. All major features documented
3. Technical dependencies listed
  </verify>
  <done>
CHANGELOG.md has Phase 7 entry documenting all scheduler changes
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md with scheduler patterns</name>
  <files>CLAUDE.md</files>
  <action>
Update CLAUDE.md to include scheduler-related patterns and conventions:

1. **Services section:** Add scheduler service
   - scheduler.py: APScheduler wrapper, singleton pattern
   - init_scheduler(): Only runs in first Gunicorn worker
   - schedule_list()/unschedule_list(): Manage individual schedules
   - validate_cron_expression(): Cron validation with cronsim

2. **Patterns section:** Document key patterns
   - Single-worker scheduler via SCHEDULER_WORKER env var
   - Jobs triggered via existing job_executor.submit_job()
   - Global pause via ServiceConfig.scheduler_paused
   - Schedule sync: routes call schedule_list/unschedule_list after DB changes

3. **Routes section:** Add schedule routes
   - /schedule: Schedule management page
   - /api/schedule/pause: Pause all schedules
   - /api/schedule/resume: Resume all schedules
   - /api/schedule/status: Get scheduler status
   - /api/dashboard/upcoming: Get upcoming jobs

4. **Dependencies section:** Add scheduler dependencies
   - APScheduler, cronsim, cron-descriptor
  </action>
  <verify>
Review CLAUDE.md to confirm:
1. Scheduler service documented
2. Key patterns explained
3. New routes listed
4. Dependencies noted
  </verify>
  <done>
CLAUDE.md updated with scheduler patterns and conventions
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. README.md includes scheduler feature documentation
2. CHANGELOG.md has Phase 7 entry
3. CLAUDE.md documents scheduler patterns and routes
4. Documentation is consistent with actual implementation
</verification>

<success_criteria>
- README documents scheduler for end users
- CHANGELOG records all Phase 7 changes
- CLAUDE.md provides AI assistant context for scheduler code
- All documentation follows existing style conventions
</success_criteria>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 7: Scheduler System with all documentation</what-built>
  <how-to-verify>
1. Create a test list with "Every hour" schedule via wizard
2. Verify it appears on /schedule page with "Scheduled" status
3. Verify Lists page shows "Next: in X minutes" subtitle
4. Verify Dashboard shows the list in Upcoming widget
5. Click global pause on Schedule page
6. Verify "Paused" status appears
7. Resume and verify "Scheduled" status returns
8. Wait for scheduled time (or create 5-minute test schedule)
9. Verify job executes automatically
10. Check Jobs page shows "Scheduled" in trigger column
  </how-to-verify>
  <resume-signal>Type "approved" if scheduler works correctly, or describe any issues</resume-signal>
</task>

<output>
After completion, create `.planning/phases/07-scheduler-system/07-06-SUMMARY.md`
</output>
