---
phase: 06.1-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - listarr/static/js/lists.js
  - listarr/static/js/wizard.js
autonomous: false

must_haves:
  truths:
    - "Run button shows Running state throughout job execution"
    - "Result toast appears when job completes"
    - "Edit save redirects to lists page with success notification"
  artifacts:
    - path: "listarr/static/js/lists.js"
      provides: "Job status polling and toast notifications"
      contains: "pollJobStatus"
    - path: "listarr/static/js/wizard.js"
      provides: "Wizard form submission and redirect"
      contains: "submitWizard"
  key_links:
    - from: "listarr/static/js/lists.js"
      to: "/api/lists/{id}/status"
      via: "pollJobStatus fetch"
      pattern: "fetch.*status"
    - from: "listarr/static/js/wizard.js"
      to: "/lists"
      via: "redirect after submit"
      pattern: "window\\.location.*lists"
---

<objective>
Verify and fix MONITOR bugs #4, #5, #6 (run status reverting, intermittent toast notifications, edit navigation).

Purpose: These bugs were marked MONITOR in research as they may have been resolved during Phase 6 Wave 2 work. This plan verifies current behavior and fixes any remaining issues.

Output: Verified working UI feedback or fixed JavaScript files if issues persist.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-bug-fixes/06.1-CONTEXT.md
@.planning/phases/06.1-bug-fixes/06.1-RESEARCH.md
@.planning/phases/06.1-bug-fixes/06.1-01-SUMMARY.md
@listarr/static/js/lists.js
@listarr/static/js/wizard.js
@listarr/static/js/toast.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify MONITOR bugs current state</name>
  <what-built>Phase 6 Wave 2 work that may have fixed these bugs</what-built>
  <how-to-verify>
**Start the application:**
```bash
cd "//10.0.0.28/Storage/Scripts/Personal/Listarr - Development"
python -m flask --app listarr run --debug
```

**Bug #4 - Run Status Reverting:**
1. Go to Lists page (http://localhost:5000/lists)
2. Click "Run" on any configured list
3. Observe: Does button show "Running" state?
4. Wait 10-15 seconds (during poll cycles)
5. Verify: Does it stay "Running" until job completes, then show result?
6. Note: If it reverts to "Run" prematurely, bug is NOT fixed

**Bug #5 - Intermittent Toast Notifications:**
1. Run 2-3 different lists sequentially (wait for each to complete)
2. Verify: Does toast notification appear for EACH completed job?
3. Note: If some toasts are missing, bug is NOT fixed

**Bug #6 - Save Edit Navigation:**
1. Go to Lists page
2. Click "Edit" on any list
3. Make a small change (e.g., change name slightly)
4. Click "Save" / submit the wizard
5. Verify: Does it redirect to /lists page?
6. Verify: Does success notification appear?
7. Note: If redirect fails or no notification, bug is NOT fixed

**Report findings:**
- Bug #4: FIXED / NOT FIXED (describe behavior)
- Bug #5: FIXED / NOT FIXED (describe behavior)
- Bug #6: FIXED / NOT FIXED (describe behavior)
  </how-to-verify>
  <resume-signal>
Report status for each bug:
- "all-fixed" - All 3 bugs working correctly, skip to Task 4
- "bug4-broken" - Run status reverting, proceed to Task 2
- "bug5-broken" - Toast notifications inconsistent, proceed to Task 3
- "bug6-broken" - Edit navigation broken, proceed to Task 3
- Or describe specific combination of issues
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fix run status reverting (if Bug #4 NOT fixed)</name>
  <files>listarr/static/js/lists.js</files>
  <action>
**Only execute this task if Bug #4 is NOT fixed from verification.**

Investigate the `pollJobStatus()` function and button state management.

Potential issues to check:
1. Is job status response being parsed correctly?
2. Is localStorage tracking job IDs correctly?
3. Is there a race condition in poll cycle vs button update?
4. Is the server returning 'idle' when job is still running?

Look for patterns like:
- Button state update happening before poll completes
- Multiple poll cycles interfering with each other
- Incorrect status comparison

If the issue is client-side, fix the JavaScript.
If the issue is server-side (get_job_status returning wrong status), note it for separate fix.

Ensure:
- Button shows "Running" when job status is 'running' or 'pending'
- Button shows result only when job status is 'completed' or 'failed'
- No premature state reversion during active polling
  </action>
  <verify>
Manually test:
1. Run a list
2. Verify "Running" state persists throughout execution
3. Verify result appears when job completes
  </verify>
  <done>
Run button maintains "Running" state throughout job execution, no premature reversion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix toast/navigation issues (if Bug #5 or #6 NOT fixed)</name>
  <files>listarr/static/js/lists.js, listarr/static/js/wizard.js</files>
  <action>
**Only execute this task if Bug #5 or #6 is NOT fixed from verification.**

**For Bug #5 (Toast Notifications):**
Check `showResultToast()` function and all code paths that should call it:
- Is toast container being created properly?
- Is `showResultToast()` called on all completion paths?
- Is there a race condition with DOM readiness?

**For Bug #6 (Edit Navigation):**
Check `submitWizard()` function in wizard.js:
- Is redirect URL correct (`/lists?success=updated`)?
- Is there an error preventing redirect?
- Is success param being checked in lists.js?

Fix any issues found. Ensure:
- Toast appears for EVERY job completion (success or failure)
- Edit save redirects to /lists with success notification
  </action>
  <verify>
Manually test:
1. Run multiple lists, verify toast for each
2. Edit a list, save, verify redirect and notification
  </verify>
  <done>
Toast notifications appear consistently. Edit save redirects correctly with success notification.
  </done>
</task>

<task type="auto">
  <name>Task 4: Final verification and test suite</name>
  <files>None (verification only)</files>
  <action>
Run full test suite to ensure no regressions:

```bash
python -m pytest tests/ -v --tb=short
```

Verify all tests pass (should be 415+ tests).

If any tests fail that are related to the bugs fixed in this phase, investigate and fix.
  </action>
  <verify>
All tests pass:
```bash
python -m pytest tests/ -v --tb=short
```
  </verify>
  <done>
All tests pass. Phase 6.1 bug fixes complete.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Bug #1 (from Plan 01):** Override tag replaces default
2. **Bug #2 (from Plan 01):** Tag table decision documented
3. **Bug #3 (from Plan 01):** Logging respects LOG_LEVEL
4. **Bug #4:** Run status shows "Running" throughout execution
5. **Bug #5:** Toast notifications appear for all job completions
6. **Bug #6:** Edit save redirects to lists page with notification

Full test suite:
```bash
python -m pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- All 6 bugs from Phase 6.1 roadmap are resolved or documented
- Run button maintains proper state throughout job lifecycle
- Toast notifications appear consistently
- Edit save navigates correctly with feedback
- All 415+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-bug-fixes/06.1-02-SUMMARY.md`
</output>
