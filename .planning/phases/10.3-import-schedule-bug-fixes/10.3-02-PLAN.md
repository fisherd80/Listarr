---
phase: 10.3-import-schedule-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - listarr/routes/schedule_routes.py
  - listarr/templates/schedule.html
  - listarr/static/js/schedule.js
  - listarr/forms/lists_forms.py
autonomous: true

must_haves:
  truths:
    - "Schedule page has an edit button for each list that opens a schedule edit modal"
    - "Schedule edit modal supports cron preset dropdown (same presets as wizard)"
    - "Schedule changes saved via modal update the database and APScheduler"
    - "Weekly cron expression in lists_forms.py uses SUN not 0"
  artifacts:
    - path: "listarr/routes/schedule_routes.py"
      provides: "POST /api/schedule/<list_id>/update endpoint"
      contains: "update_schedule"
    - path: "listarr/templates/schedule.html"
      provides: "Schedule edit modal with cron preset selector"
      contains: "schedule-edit-modal"
    - path: "listarr/static/js/schedule.js"
      provides: "Modal open/close and save handlers"
      contains: "openEditModal"
    - path: "listarr/forms/lists_forms.py"
      provides: "Fixed weekly cron preset"
      contains: "0 0 * * SUN"
  key_links:
    - from: "listarr/static/js/schedule.js"
      to: "/api/schedule/<list_id>/update"
      via: "fetch POST on save"
      pattern: "api/schedule.*update"
    - from: "listarr/routes/schedule_routes.py"
      to: "listarr/services/scheduler.py"
      via: "schedule_list/unschedule_list calls"
      pattern: "schedule_list|unschedule_list"
---

<objective>
Add dedicated schedule edit modal to schedule page and fix weekly cron expression in edit form.

Purpose: The schedule page is read-only — users must navigate away to the list wizard to change schedules. This adds an inline edit modal for quick schedule changes without leaving the page. Also fixes the weekly cron preset in lists_forms.py which still uses numeric "0 0 * * 0" (APScheduler interprets 0 as Monday) instead of text "0 0 * * SUN".

Output: Schedule page with edit buttons that open a modal with cron preset selector, saving changes inline. Fixed cron preset in lists_forms.py.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/resolved/schedule-edit-and-nextrun.md

Source files to modify:
@listarr/routes/schedule_routes.py
@listarr/templates/schedule.html
@listarr/static/js/schedule.js
@listarr/forms/lists_forms.py
@listarr/services/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schedule update API endpoint and fix cron preset</name>
  <files>
    listarr/routes/schedule_routes.py
    listarr/forms/lists_forms.py
  </files>
  <action>
  **lists_forms.py — Fix weekly cron expression:**

  Change line 11 from:
  ```python
  ("0 0 * * 0", "Weekly (Sunday midnight)"),
  ```
  to:
  ```python
  ("0 0 * * SUN", "Weekly (Sunday midnight)"),
  ```

  This matches the wizard template fix already applied (commit 0bb0ca7). APScheduler uses ISO weekday numbering where 0=Monday, but all three libraries (APScheduler, cronsim, cron-descriptor) correctly interpret "SUN" as Sunday.

  **schedule_routes.py — Add POST /api/schedule/<list_id>/update endpoint:**

  Add this route after the `get_schedule_status()` function (after line 190):

  ```python
  @bp.route("/api/schedule/<int:list_id>/update", methods=["POST"])
  @csrf.exempt
  def update_schedule(list_id):
      """
      Update the schedule for a specific list.

      Accepts JSON: {"schedule_cron": "0 0 * * *"} or {"schedule_cron": ""} to remove schedule.

      Returns:
          JSON: {success: true, schedule_cron: "...", status: "...", next_run: "..."}
      """
      from flask import current_app, request

      from listarr.services.scheduler import schedule_list, unschedule_list, validate_cron_expression

      list_obj = List.query.get(list_id)
      if not list_obj:
          return jsonify({"success": False, "error": "List not found"}), 404

      data = request.get_json()
      if data is None:
          return jsonify({"success": False, "error": "Invalid JSON"}), 400

      new_cron = data.get("schedule_cron", "").strip()

      try:
          # Validate cron expression if provided
          if new_cron:
              validation = validate_cron_expression(new_cron)
              if not validation["valid"]:
                  return jsonify({"success": False, "error": f"Invalid cron: {validation['error']}"}), 400

          # Update database
          old_cron = list_obj.schedule_cron
          list_obj.schedule_cron = new_cron if new_cron else None
          db.session.commit()

          # Update scheduler
          try:
              if new_cron and list_obj.is_active:
                  schedule_list(list_obj.id, new_cron)
              else:
                  unschedule_list(list_obj.id)
          except Exception as e:
              current_app.logger.warning(f"Scheduler update failed for list {list_id}: {e}")

          # Get updated status
          config = ServiceConfig.query.first()
          scheduler_paused = config.scheduler_paused if config else False
          status = _get_list_status(list_obj, scheduler_paused)

          next_run = None
          if new_cron and not scheduler_paused:
              from listarr.services.scheduler import get_next_run_time
              next_run_dt = get_next_run_time(list_obj.id)
              if next_run_dt:
                  next_run = next_run_dt.isoformat()

          return jsonify({
              "success": True,
              "schedule_cron": new_cron or "",
              "status": status,
              "next_run": next_run,
          })

      except Exception as e:
          db.session.rollback()
          current_app.logger.error(f"Error updating schedule for list {list_id}: {e}", exc_info=True)
          return jsonify({"success": False, "error": "Failed to update schedule"}), 500
  ```

  Add the missing import at the top of schedule_routes.py:
  ```python
  from listarr import csrf, db
  ```
  (Currently only imports `csrf`, needs `db` for session commit/rollback.)
  </action>
  <verify>
  - Grep for `0 0 * * 0` in lists_forms.py returns NO results
  - Grep for `0 0 * * SUN` in lists_forms.py returns 1 result
  - Grep for `update_schedule` in schedule_routes.py returns results
  - `pytest tests/ -v` — all existing tests pass
  </verify>
  <done>
  - Weekly cron expression in lists_forms.py uses "0 0 * * SUN"
  - POST /api/schedule/<list_id>/update endpoint exists
  - Endpoint validates cron, updates DB, syncs scheduler, returns new status
  - Endpoint handles empty cron (removes schedule)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add schedule edit modal UI and JavaScript handlers</name>
  <files>
    listarr/templates/schedule.html
    listarr/static/js/schedule.js
  </files>
  <action>
  **schedule.html — Add edit button column and modal:**

  1. Add an "Actions" column header to the table (after the "Next Run" th):
     ```html
     <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 dark:text-gray-300 uppercase tracking-wider">
       Actions
     </th>
     ```

  2. Add an edit button cell at the end of each table row (before the closing `</tr>`), AFTER the next_run `<td>`:
     ```html
     <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
       <button
         type="button"
         class="text-indigo-600 hover:text-indigo-900 dark:text-indigo-400 dark:hover:text-indigo-300"
         onclick="event.stopPropagation(); openEditModal({{ list_item.id }}, '{{ list_item.name }}', '{{ list_item.schedule_cron or '' }}')"
         title="Edit schedule"
       >
         <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
           <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
         </svg>
       </button>
     </td>
     ```

     Note: `event.stopPropagation()` prevents the row onclick from firing when clicking the edit button.

  3. Add `schedule_cron` to the data passed in `schedule_data` in schedule_routes.py. In the `schedule_data.append(...)` block in `schedule_page()`, add:
     ```python
     "schedule_cron": list_obj.schedule_cron or "",
     ```
     Also add this same field to the `schedule_data.append(...)` in `get_schedule_status()`.

  4. Add a modal dialog BEFORE the closing `{% endblock %}` in schedule.html:
     ```html
     <!-- Schedule Edit Modal -->
     <div id="schedule-edit-modal" class="hidden fixed inset-0 z-50 overflow-y-auto">
       <div class="flex items-center justify-center min-h-screen px-4">
         <!-- Backdrop -->
         <div class="fixed inset-0 bg-black bg-opacity-50 transition-opacity" onclick="closeEditModal()"></div>

         <!-- Modal content -->
         <div class="relative bg-white dark:bg-gray-800 rounded-lg shadow-xl max-w-md w-full p-6">
           <div class="flex justify-between items-center mb-4">
             <h3 class="text-lg font-medium text-gray-900 dark:text-gray-100" id="modal-title">
               Edit Schedule
             </h3>
             <button type="button" onclick="closeEditModal()" class="text-gray-400 hover:text-gray-500 dark:hover:text-gray-300">
               <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
               </svg>
             </button>
           </div>

           <input type="hidden" id="modal-list-id" value="">

           <div class="space-y-4">
             <div>
               <label for="modal-schedule-select" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                 Schedule
               </label>
               <select id="modal-schedule-select" class="w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-2 px-3">
                 <option value="">Manual only</option>
                 <option value="0 * * * *">Every hour</option>
                 <option value="0 */6 * * *">Every 6 hours</option>
                 <option value="0 0 * * *">Daily (midnight)</option>
                 <option value="0 0 * * SUN">Weekly (Sunday midnight)</option>
               </select>
             </div>

             <div id="modal-cron-input-wrapper" class="hidden">
               <label for="modal-cron-input" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                 Custom cron expression
               </label>
               <input
                 type="text"
                 id="modal-cron-input"
                 placeholder="e.g., 0 6 * * MON"
                 class="w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 py-2 px-3"
               >
               <p class="mt-1 text-xs text-gray-500 dark:text-gray-400">Standard 5-field cron: minute hour day-of-month month day-of-week</p>
             </div>

             <div class="flex items-center">
               <button type="button" id="modal-toggle-custom" onclick="toggleCustomCron()" class="text-sm text-indigo-600 dark:text-indigo-400 hover:text-indigo-800 dark:hover:text-indigo-300">
                 Use custom cron expression
               </button>
             </div>
           </div>

           <div class="mt-6 flex justify-end space-x-3">
             <button
               type="button"
               onclick="closeEditModal()"
               class="px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm hover:bg-gray-50 dark:hover:bg-gray-600"
             >
               Cancel
             </button>
             <button
               type="button"
               id="modal-save-btn"
               onclick="saveSchedule()"
               class="px-4 py-2 text-sm font-medium text-white bg-indigo-600 border border-transparent rounded-md shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
             >
               Save
             </button>
           </div>
         </div>
       </div>
     </div>
     ```

  **schedule.js — Add modal handlers:**

  Add the following functions before the `document.addEventListener("DOMContentLoaded", ...)` block:

  ```javascript
  /**
   * State for the edit modal.
   */
  const modalState = {
    listId: null,
    useCustom: false,
  };

  /**
   * Open the schedule edit modal for a list.
   * @param {number} listId - List ID
   * @param {string} listName - List name for modal title
   * @param {string} currentCron - Current cron expression
   */
  function openEditModal(listId, listName, currentCron) {
    modalState.listId = listId;
    modalState.useCustom = false;

    document.getElementById("modal-list-id").value = listId;
    document.getElementById("modal-title").textContent = `Edit Schedule: ${escapeHtml(listName)}`;

    // Try to match current cron to a preset
    const select = document.getElementById("modal-schedule-select");
    const customInput = document.getElementById("modal-cron-input");
    const customWrapper = document.getElementById("modal-cron-input-wrapper");
    const toggleBtn = document.getElementById("modal-toggle-custom");

    let matchedPreset = false;
    for (const option of select.options) {
      if (option.value === currentCron) {
        select.value = currentCron;
        matchedPreset = true;
        break;
      }
    }

    if (!matchedPreset && currentCron) {
      // Custom cron expression — show custom input
      modalState.useCustom = true;
      select.classList.add("hidden");
      customWrapper.classList.remove("hidden");
      customInput.value = currentCron;
      toggleBtn.textContent = "Use preset schedule";
    } else {
      select.classList.remove("hidden");
      customWrapper.classList.add("hidden");
      customInput.value = "";
      toggleBtn.textContent = "Use custom cron expression";
    }

    // Show modal
    document.getElementById("schedule-edit-modal").classList.remove("hidden");
  }

  /**
   * Close the schedule edit modal.
   */
  function closeEditModal() {
    document.getElementById("schedule-edit-modal").classList.add("hidden");
    modalState.listId = null;
  }

  /**
   * Toggle between preset dropdown and custom cron input.
   */
  function toggleCustomCron() {
    modalState.useCustom = !modalState.useCustom;
    const select = document.getElementById("modal-schedule-select");
    const customWrapper = document.getElementById("modal-cron-input-wrapper");
    const toggleBtn = document.getElementById("modal-toggle-custom");

    if (modalState.useCustom) {
      select.classList.add("hidden");
      customWrapper.classList.remove("hidden");
      toggleBtn.textContent = "Use preset schedule";
    } else {
      select.classList.remove("hidden");
      customWrapper.classList.add("hidden");
      toggleBtn.textContent = "Use custom cron expression";
    }
  }

  /**
   * Save the schedule from the modal.
   */
  async function saveSchedule() {
    const listId = modalState.listId;
    if (!listId) return;

    let cronValue;
    if (modalState.useCustom) {
      cronValue = document.getElementById("modal-cron-input").value.trim();
    } else {
      cronValue = document.getElementById("modal-schedule-select").value;
    }

    const saveBtn = document.getElementById("modal-save-btn");
    saveBtn.disabled = true;
    saveBtn.textContent = "Saving...";

    try {
      const response = await fetch(`/api/schedule/${listId}/update`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-CSRFToken": getCsrfToken(),
        },
        body: JSON.stringify({ schedule_cron: cronValue }),
      });

      if (!response.ok) {
        const errorData = await response.json().catch(() => ({}));
        throw new Error(errorData.error || `HTTP ${response.status}`);
      }

      const data = await response.json();
      if (!data.success) throw new Error(data.error || "Save failed");

      // Close modal and refresh status
      closeEditModal();
      await refreshScheduleStatus();

      // Reload page to get updated data (simplest approach for table update)
      window.location.reload();
    } catch (error) {
      console.error("Failed to save schedule:", error);
      if (window.showToast) {
        window.showToast(error.message || "Failed to save schedule", "error");
      }
    } finally {
      saveBtn.disabled = false;
      saveBtn.textContent = "Save";
    }
  }
  ```

  Also add Escape key handler inside the DOMContentLoaded listener:
  ```javascript
  // Close modal on Escape key
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") closeEditModal();
  });
  ```
  </action>
  <verify>
  - Grep for `schedule-edit-modal` in schedule.html returns results
  - Grep for `openEditModal` in schedule.js returns results
  - Grep for `update_schedule` in schedule_routes.py returns results
  - Grep for `0 0 * * SUN` in lists_forms.py returns 1 result
  - Grep for `schedule_cron` in schedule_routes.py — appears in both schedule_page() and get_schedule_status() data
  - `pytest tests/ -v` — all tests pass
  </verify>
  <done>
  - Schedule page has edit button (pencil icon) on each row
  - Edit button opens modal with preset dropdown (Manual, Every hour, Every 6 hours, Daily, Weekly)
  - Modal has toggle to switch to custom cron input
  - Save button calls POST /api/schedule/<list_id>/update
  - Modal handles Escape key and backdrop click to close
  - Weekly cron in lists_forms.py fixed to "0 0 * * SUN"
  - schedule_cron field included in schedule_data for both page render and API status
  - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ -v` — all tests pass
2. Visit `/schedule` — edit button visible on each table row
3. Click edit button — modal opens with correct current schedule selected
4. Change schedule via preset — save updates status and next run in table
5. Use custom cron input — save validates and applies expression
6. Set to "Manual only" — removes schedule from list
7. Grep: no `0 0 * * 0` remains in lists_forms.py
</verification>

<success_criteria>
- Schedule page has clickable edit button per row (pencil icon)
- Edit modal opens with current schedule pre-selected
- Modal supports preset dropdown and custom cron input toggle
- Save persists to database and updates APScheduler
- Weekly cron preset uses "0 0 * * SUN" in lists_forms.py
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10.3-import-schedule-bug-fixes/10.3-02-SUMMARY.md`
</output>
