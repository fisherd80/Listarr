---
phase: 09.1-config-deduplication
plan: 03
subsystem: frontend
tags: [javascript, refactoring, deduplication, config-ui]

# Dependency graph
requires:
  - phase: 09.1-config-deduplication
    plan: 01
    provides: Parameterized config routes (/config/<service>/*)
provides:
  - Parameterized config.js with 3 fetch + 2 handler functions replacing 12 duplicates
  - Shared utils.js with formatTimestamp and generateStatusHTML
  - settings.js cleaned of duplicate utility functions
affects: [config-ui, settings-ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Service-parameterized JS functions using template literals for DOM IDs and fetch URLs
    - Shared utility module (utils.js) included before page-specific scripts

key-files:
  created:
    - listarr/static/js/utils.js
  modified:
    - listarr/static/js/config.js
    - listarr/static/js/settings.js
    - listarr/templates/config.html
    - listarr/templates/settings.html

key-decisions:
  - "Parameterized JS functions use service string for DOM IDs (${service}-root-folder) and fetch URLs (/config/${service}/)"
  - "Test connection URLs kept as /config/test_${service}_api (matching Plan 01 backward-compat aliases)"
  - "dataLoaded tracking uses object { radarr: false, sonarr: false } instead of separate variables"
  - "utils.js loaded before page scripts via separate <script> tag (no module bundler)"

patterns-established:
  - "Service-parameterized JS: pass 'radarr' or 'sonarr' string to shared functions"
  - "Shared utility extraction: utils.js for cross-page helpers"

# Metrics
duration: 3min
completed: 2026-02-06
---

# Phase 09.1 Plan 03: Config.js & Utils.js Consolidation Summary

**Consolidated config.js from 746 to 322 lines (57% reduction) by parameterizing 12 duplicate functions into 6, and extracted shared utilities to utils.js**

## Performance

- **Duration:** 3 min
- **Completed:** 2026-02-06
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Created utils.js with formatTimestamp and generateStatusHTML (shared by config and settings pages)
- Replaced 6 duplicate fetch functions with 3 parameterized versions (fetchRootFolders, fetchQualityProfiles, loadSavedSettings)
- Consolidated 2 duplicate test connection handlers into setupTestConnection(service)
- Consolidated 2 duplicate save handlers into setupSaveImportSettings(service)
- Replaced separate dataLoaded flags with single tracking object
- Updated config.html and settings.html to include utils.js
- Removed 23 lines of duplicate utility functions from settings.js
- config.js reduced from 746 to 322 lines (57% reduction, 424 lines removed)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract shared JS utilities to utils.js** - `70d9bdb` (refactor)
2. **Task 2: Consolidate duplicate config.js functions** - `07111c9` (refactor)

## Files Created/Modified
- `listarr/static/js/utils.js` - NEW: Shared formatTimestamp and generateStatusHTML
- `listarr/static/js/config.js` - Parameterized config page JavaScript (746 -> 322 lines)
- `listarr/static/js/settings.js` - Removed duplicate utility functions (82 -> 59 lines)
- `listarr/templates/config.html` - Added utils.js script include
- `listarr/templates/settings.html` - Added utils.js script include

## Decisions Made
- **Template literal DOM IDs:** Functions use `${service}-root-folder` pattern matching existing HTML element IDs
- **Test connection URL pattern:** Uses `/config/test_${service}_api` matching Plan 01's backward-compatible aliases
- **No module bundler:** utils.js loaded via plain `<script>` tag before page scripts (keeps build-free simplicity)
- **Sonarr season_folder conditional:** Preserved in both loadSavedSettings and setupSaveImportSettings via `if (service === "sonarr")` checks

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

None

## User Setup Required

None

## Phase 9.1 Completion

This was the final plan (3/3) in Phase 9.1. Phase results:
- **config_routes.py:** 897 -> 404 lines (55% reduction)
- **config.js:** 746 -> 322 lines (57% reduction)
- **~920 lines of duplication eliminated** across Python routes and JavaScript
- **utils.js created** as shared utility module
- **All 453 tests pass**

---
*Phase: 09.1-config-deduplication*
*Completed: 2026-02-07*
