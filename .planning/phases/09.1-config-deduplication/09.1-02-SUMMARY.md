---
phase: 09.1-config-deduplication
plan: 02
subsystem: testing
tags: [pytest, mocks, refactoring, arr-service]

# Dependency graph
requires:
  - phase: 09.1-config-deduplication
    plan: 01
    provides: Parameterized config_routes.py with arr_service shared functions
provides:
  - Updated mock paths in test_config_routes.py (92 tests)
  - Updated mock paths in test_config_integration.py (23 tests)
  - All 453 tests passing with refactored config routes
affects: [09.1-03, test-suite]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Single validate_api_key mock replaces service-specific validate mocks
    - Shared get_quality_profiles/get_root_folders mock paths for both services

key-files:
  created: []
  modified:
    - tests/routes/test_config_routes.py
    - tests/integration/test_config_integration.py

key-decisions:
  - "Collapsed dual validate_api_key patches into single patch where both Radarr and Sonarr tested together"
  - "Helper function tests updated to call _test_and_update_service_status directly with service parameter"
  - "Integration tests follow same mock path pattern as unit tests (arr_service shared functions)"

patterns-established:
  - "Mock path pattern: listarr.routes.config_routes.{shared_function} (not service-specific)"

# Metrics
duration: 2min
completed: 2026-02-07
---

# Phase 09.1 Plan 02: Test Mock Path Updates Summary

**Updated mock paths in 115 config tests across route and integration test files to match Plan 01's parameterized config_routes.py**

## Performance

- **Duration:** 2 min
- **Completed:** 2026-02-07
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Replaced all `validate_radarr_api_key` / `validate_sonarr_api_key` mock paths with `validate_api_key`
- Replaced all `get_radarr_quality_profiles` / `get_sonarr_quality_profiles` with `get_quality_profiles`
- Replaced all `get_radarr_root_folders` / `get_sonarr_root_folders` with `get_root_folders`
- Updated helper function tests from `_test_and_update_radarr_status` / `_test_and_update_sonarr_status` to `_test_and_update_service_status`
- Collapsed dual `validate_api_key` patches into single patch in `TestMultipleRequestsScenarios`
- Full test suite: 453 passed, 0 failed

## Task Commits

Each task was committed atomically:

1. **Task 1: Update mock paths and function references for parameterized routes** - `962ba33` (test)

## Files Created/Modified
- `tests/routes/test_config_routes.py` - 92 tests with updated mock paths
- `tests/integration/test_config_integration.py` - 23 tests with updated mock paths

## Decisions Made
- **Single validate_api_key patch:** Where tests previously patched both `validate_radarr_api_key` and `validate_sonarr_api_key` separately, collapsed to single `validate_api_key` patch since it's now one shared function.
- **Helper tests use service parameter:** Tests for `_test_and_update_service_status` pass "RADARR" or "SONARR" as first argument instead of importing deleted wrapper functions.

## Deviations from Plan
- **Integration tests also needed updates** (auto-fix blocker): Plan 02 scoped only `tests/routes/test_config_routes.py`, but `tests/integration/test_config_integration.py` had 22 failing tests with identical stale mock paths. Fixed both files to achieve zero failures.

## Issues Encountered

None

## User Setup Required

None

## Next Phase Readiness

- All 453 tests pass with parameterized config routes
- Ready for Plan 03 (config.js JavaScript consolidation)
- No test infrastructure changes needed

---
*Phase: 09.1-config-deduplication*
*Completed: 2026-02-07*
