---
phase: 09.1-config-deduplication
plan: 02
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - tests/routes/test_config_routes.py
autonomous: true

must_haves:
  truths:
    - "All 453+ tests pass after config_routes.py refactor"
    - "Tests cover parameterized routes for both radarr and sonarr"
    - "Tests for deleted wrapper functions are removed or updated"
    - "Mock paths match new import structure (arr_service instead of service-specific)"
  artifacts:
    - path: "tests/routes/test_config_routes.py"
      provides: "Complete test coverage for refactored config routes"
      contains: "test_fetch_quality_profiles"
  key_links:
    - from: "tests/routes/test_config_routes.py"
      to: "listarr/routes/config_routes.py"
      via: "mock patches and URL requests"
      pattern: "listarr\\.routes\\.config_routes"
---

<objective>
Update the test file to match the refactored config_routes.py from Plan 01. Tests must pass with the new parameterized routes, updated imports, and removed wrapper functions.

Purpose: The 1835-line test file references specific function names, mock paths, and URL patterns that changed in Plan 01. Without updating tests, the test suite will break.

Output: All tests pass with the refactored config_routes.py.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-config-deduplication/09.1-01-SUMMARY.md
@tests/routes/test_config_routes.py
@listarr/routes/config_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mock paths and function references for parameterized routes</name>
  <files>tests/routes/test_config_routes.py</files>
  <action>
Read the refactored config_routes.py (from Plan 01) to understand the exact new function names and import paths, then update all test mocks accordingly.

**Changes required based on Plan 01 refactoring:**

1. **Update mock paths for arr_service functions.** Plan 01 replaced service-specific imports:
   - OLD: `@patch("listarr.routes.config_routes.get_radarr_quality_profiles")`
   - NEW: `@patch("listarr.routes.config_routes.get_quality_profiles")`

   Same for: get_root_folders, get_tags, create_or_get_tag_id.

   Apply this transformation to ALL test classes that mock these functions:
   - TestRadarrQualityProfilesEndpoint: `get_radarr_quality_profiles` -> `get_quality_profiles`
   - TestRadarrRootFoldersEndpoint: `get_radarr_root_folders` -> `get_root_folders`
   - TestRadarrImportSettingsEndpoints: `get_radarr_root_folders` -> `get_root_folders`
   - TestSonarrQualityProfilesEndpoint: `get_sonarr_quality_profiles` -> `get_quality_profiles`
   - TestSonarrRootFoldersEndpoint: `get_sonarr_root_folders` -> `get_root_folders`
   - TestSonarrImportSettingsEndpoints: `get_sonarr_root_folders` -> `get_root_folders`

2. **Update mock paths for validate functions.** Plan 01 replaced:
   - OLD: `@patch("listarr.routes.config_routes.validate_radarr_api_key")`
   - NEW: `@patch("listarr.routes.config_routes.validate_api_key")`

   Apply to ALL test classes that mock validation:
   - TestRadarrConfigPOST: All `validate_radarr_api_key` -> `validate_api_key`
   - TestSonarrConfigPOST: All `validate_sonarr_api_key` -> `validate_api_key`
   - TestTestRadarrAPIAjax: All `validate_radarr_api_key` -> `validate_api_key`
   - TestTestSonarrAPIAjax: All `validate_sonarr_api_key` -> `validate_api_key`
   - TestConcurrentOperations: Both `validate_radarr_api_key` and `validate_sonarr_api_key` -> `validate_api_key`
   - TestHelperFunctions: Both validate functions -> `validate_api_key`

   IMPORTANT: When a test patches `validate_api_key` for BOTH Radarr and Sonarr in the same test (like TestConcurrentOperations.test_save_both_services_in_single_post), only ONE patch is needed since it's now the same function. But the mock needs to return True for both calls. Use a single `@patch("listarr.routes.config_routes.validate_api_key")` with `mock.return_value = True`.

3. **Update helper function tests.** Plan 01 deleted `_test_and_update_radarr_status` and `_test_and_update_sonarr_status` wrapper functions. Update TestHelperFunctions:
   - `test_helper_test_and_update_radarr_status_returns_success_tuple`: Change to import `_test_and_update_service_status` directly and call it with `("RADARR", url, key)`.
   - `test_helper_test_and_update_sonarr_status_returns_failure_tuple`: Same, call with `("SONARR", url, key)`.
   - `test_helper_test_and_update_radarr_status_database_error`: Update to use `_test_and_update_service_status`.
   - `test_helper_test_and_update_sonarr_status_database_error`: Update to use `_test_and_update_service_status`.

   Rename the test methods to reflect the generic function name (e.g., `test_helper_test_and_update_service_status_radarr_success`).

4. **Verify test URLs still work.** The parameterized routes use `<service>` in the URL, so `/config/radarr/quality-profiles` still works (Flask matches "radarr" as the service parameter). The test URLs do NOT need to change for quality-profiles, root-folders, and import-settings endpoints. Verify by reading the refactored routes.

5. **Update test_radarr_api / test_sonarr_api URL tests.** Plan 01 kept backward-compatible aliases for `/config/test_radarr_api` and `/config/test_sonarr_api`. The existing test URLs should still work. Just update the mock paths.

6. **Update "not configured" message assertions.** Plan 01 parameterized the "not configured" message to use capitalized service name (e.g., "Radarr not configured" or "Sonarr not configured"). Check that test assertions match the new message format. The current tests already check for "Radarr not configured" / "Sonarr not configured" so these should be fine -- but verify against the actual refactored code.

7. **Update test file docstring.** Update the module docstring at the top to reflect the refactored structure (parameterized routes, arr_service imports).

After making all changes, run the full test suite to verify all tests pass.
  </action>
  <verify>
Run `python -m pytest tests/routes/test_config_routes.py -v` -- all tests must pass.
Run `python -m pytest` -- all 453+ tests must pass (no regressions).
  </verify>
  <done>
All tests in test_config_routes.py pass with the refactored config_routes.py. Mock paths reference arr_service shared functions. No references to deleted wrapper functions remain. Full test suite (453+ tests) passes with zero failures.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/routes/test_config_routes.py -v` passes all tests
- `python -m pytest` passes 453+ tests with zero failures
- `grep "validate_radarr_api_key\|validate_sonarr_api_key\|get_radarr_quality_profiles\|get_sonarr_quality_profiles\|get_radarr_root_folders\|get_sonarr_root_folders" tests/routes/test_config_routes.py` returns nothing (no old mock paths)
- `grep "_test_and_update_radarr_status\|_test_and_update_sonarr_status" tests/routes/test_config_routes.py` returns nothing (no references to deleted wrappers)
</verification>

<success_criteria>
- Zero test failures in test_config_routes.py
- Zero test failures in full test suite
- All mock paths updated to match new import structure
- Helper function tests updated to use _test_and_update_service_status directly
- No references to deleted functions or old import paths remain
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-config-deduplication/09.1-02-SUMMARY.md`
</output>
