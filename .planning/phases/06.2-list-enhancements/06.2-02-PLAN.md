---
phase: 06.2-list-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - listarr/models/service_config_model.py
  - listarr/forms/settings_forms.py
  - listarr/routes/settings_routes.py
  - listarr/templates/settings.html
  - listarr/static/js/wizard.js
  - listarr/templates/list_wizard.html
autonomous: true

must_haves:
  truths:
    - "TMDB region setting can be saved in Settings page"
    - "Region dropdown shows top 5 countries with searchable full list"
    - "Default limit for new lists is 100 items"
    - "Limit options are 25, 50, 100, 250, 500, MAX(1000)"
    - "Warning appears when selecting 500 or MAX limit"
    - "Legacy 10/20 limits are migrated to 25 when editing"
  artifacts:
    - path: "listarr/models/service_config_model.py"
      provides: "tmdb_region column on ServiceConfig"
      contains: "tmdb_region"
    - path: "listarr/forms/settings_forms.py"
      provides: "Region select field for TMDB settings"
      contains: "tmdb_region"
    - path: "listarr/static/js/wizard.js"
      provides: "Updated limit options and warning logic"
      contains: "1000"
  key_links:
    - from: "listarr/routes/settings_routes.py"
      to: "listarr/models/service_config_model.py"
      via: "Save/load tmdb_region"
      pattern: "tmdb_region"
    - from: "listarr/templates/settings.html"
      to: "listarr/forms/settings_forms.py"
      via: "Region dropdown field"
      pattern: "tmdb_region"
---

<objective>
Add TMDB region setting and update list limit options with new defaults.

Purpose: Allow users to configure TMDB region for localized results and expand list size options with sensible defaults and performance warnings.

Output: Region dropdown in Settings TMDB section persisted to database; wizard limit dropdown updated with new options (25-1000), default 100, and warning for large selections.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-list-enhancements/06.2-CONTEXT.md
@.planning/phases/06.2-list-enhancements/06.2-RESEARCH.md
@listarr/models/service_config_model.py
@listarr/forms/settings_forms.py
@listarr/routes/settings_routes.py
@listarr/templates/settings.html
@listarr/static/js/wizard.js
@listarr/templates/list_wizard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tmdb_region column to ServiceConfig model</name>
  <files>listarr/models/service_config_model.py</files>
  <action>
Add a nullable tmdb_region column to the ServiceConfig model for storing the user's preferred TMDB region.

In the ServiceConfig class, after the last_test_status column and before created_at, add:

```python
# TMDB-specific settings
tmdb_region = db.Column(db.String(2), nullable=True)  # ISO 3166-1 Alpha-2 code
```

This column:
- Uses String(2) for ISO 3166-1 Alpha-2 country codes (e.g., "US", "GB", "DE")
- Is nullable (NULL means "Worldwide" / no region filter)
- Only applies to the TMDB service config row

Note: Since the project uses db.create_all() for schema management, the column will be created on next app startup. For existing databases, a manual ALTER TABLE may be needed but that's outside scope of this plan.
  </action>
  <verify>
Run: `python -c "from listarr.models.service_config_model import ServiceConfig; print('tmdb_region' in [c.name for c in ServiceConfig.__table__.columns])"`
Should print "True".
  </verify>
  <done>
ServiceConfig model has tmdb_region column defined as nullable String(2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add region field to settings form and route</name>
  <files>listarr/forms/settings_forms.py, listarr/routes/settings_routes.py, listarr/templates/settings.html</files>
  <action>
**In listarr/forms/settings_forms.py:**

First, check if this file exists and what forms are defined. If TMDBApiForm exists, add a region field to it. If not, create the form structure.

Add a SelectField for region with these choices:
```python
# Top 5 most common regions first, then separator, then full alphabetical list
REGION_CHOICES = [
    ('', 'Worldwide (No filter)'),
    ('US', 'United States'),
    ('GB', 'United Kingdom'),
    ('CA', 'Canada'),
    ('AU', 'Australia'),
    ('DE', 'Germany'),
    # Full list (sorted alphabetically) - add all ISO 3166-1 countries
    # For brevity, include top 20 most common media regions:
    ('AR', 'Argentina'),
    ('AT', 'Austria'),
    ('BE', 'Belgium'),
    ('BR', 'Brazil'),
    ('CH', 'Switzerland'),
    ('ES', 'Spain'),
    ('FR', 'France'),
    ('IN', 'India'),
    ('IT', 'Italy'),
    ('JP', 'Japan'),
    ('KR', 'South Korea'),
    ('MX', 'Mexico'),
    ('NL', 'Netherlands'),
    ('NZ', 'New Zealand'),
    ('PL', 'Poland'),
    ('PT', 'Portugal'),
    ('RU', 'Russia'),
    ('SE', 'Sweden'),
    ('ZA', 'South Africa'),
]

tmdb_region = SelectField('Region', choices=REGION_CHOICES)
```

**In listarr/routes/settings_routes.py:**

Find the route that handles TMDB settings (likely settings_page or a dedicated TMDB route). Update it to:

1. Load existing region from ServiceConfig where service='TMDB':
   ```python
   tmdb_config = ServiceConfig.query.filter_by(service='TMDB').first()
   if tmdb_config:
       tmdb_api_form.tmdb_region.data = tmdb_config.tmdb_region or ''
   ```

2. Save region when form is submitted:
   ```python
   if 'save_api_key' in request.form and tmdb_api_form.validate_on_submit():
       # ... existing API key save logic ...
       # Add region save:
       if tmdb_config:
           tmdb_config.tmdb_region = tmdb_api_form.tmdb_region.data or None
       # ... db.session.commit() ...
   ```

**In listarr/templates/settings.html:**

In the TMDB API Configuration section, after the API key field and before the action buttons, add a region dropdown:

```html
<!-- Region Setting -->
<div class="relative max-w-lg">
  <label
    for="tmdb_region"
    class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
  >
    Region
  </label>
  <select
    id="tmdb_region"
    name="{{ tmdb_api_form.tmdb_region.name }}"
    class="w-full border border-gray-300 dark:border-gray-700 rounded-md px-3 py-2 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:ring-primary focus:border-primary"
  >
    {% for value, label in tmdb_api_form.tmdb_region.choices %}
    <option value="{{ value }}" {% if tmdb_api_form.tmdb_region.data == value %}selected{% endif %}>{{ label }}</option>
    {% endfor %}
  </select>
  <p class="mt-1 text-xs text-gray-500 dark:text-gray-400">
    Filter TMDB results by region. Leave as "Worldwide" for all regions.
  </p>
</div>
```
  </action>
  <verify>
Run Flask app and navigate to Settings page. Verify:
1. Region dropdown appears below TMDB API key
2. Selecting a region and clicking Save persists the value
3. Page reload shows saved region value

Run: `pytest tests/ -v -k "settings" --tb=short` (if settings tests exist)
  </verify>
  <done>
- REGION_CHOICES defined with Worldwide + top 5 + extended list
- tmdb_region SelectField added to TMDB settings form
- settings_routes loads and saves tmdb_region
- settings.html displays region dropdown with proper styling
  </done>
</task>

<task type="auto">
  <name>Task 3: Update wizard limit options and add warning</name>
  <files>listarr/static/js/wizard.js, listarr/templates/list_wizard.html</files>
  <action>
**In listarr/static/js/wizard.js:**

1. Update the wizardState.filters.limit default from 20 to 100:
   ```javascript
   filters: {
       // ... other fields ...
       limit: 100,  // Changed from 20
   },
   ```

2. Add a function to handle limit change with warning:
   ```javascript
   /**
    * Handle limit select change with warning for large values
    */
   function handleLimitChange() {
       const limitSelect = document.getElementById("filter-limit");
       const warningEl = document.getElementById("limit-warning");
       if (!limitSelect) return;

       const limit = parseInt(limitSelect.value, 10);
       wizardState.filters.limit = limit;

       // Show warning for large limits (500 or 1000)
       if (warningEl) {
           if (limit >= 500) {
               warningEl.classList.remove("hidden");
           } else {
               warningEl.classList.add("hidden");
           }
       }

       onFiltersChanged();
   }
   ```

3. In loadExistingListData(), add migration logic for legacy limits:
   ```javascript
   // Migrate legacy limits (10, 20 -> 25)
   let limit = existingList.limit || 100;
   if (limit === 10 || limit === 20) {
       limit = 25;  // Silent migration
   }
   wizardState.filters.limit = limit;
   ```

4. In populateStep2EditMode(), update limit dropdown to handle migrated values:
   ```javascript
   // Limit select - handle migrated values
   const limitSelect = document.getElementById("filter-limit");
   if (limitSelect && wizardState.filters.limit) {
       // If the limit doesn't exist in dropdown options, it was migrated
       const optionExists = Array.from(limitSelect.options).some(
           opt => parseInt(opt.value, 10) === wizardState.filters.limit
       );
       if (optionExists) {
           limitSelect.value = wizardState.filters.limit;
       } else {
           // Migrated value - set to closest option
           limitSelect.value = '25';
       }
       // Trigger warning check
       handleLimitChange();
   }
   ```

**In listarr/templates/list_wizard.html:**

Replace the existing limit select (around line 336-343) with updated options:

```html
<!-- Results Limit -->
<div>
  <label for="filter-limit" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
    Maximum Results
  </label>
  <select id="filter-limit"
    class="filter-input w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm text-gray-900 dark:text-gray-100 bg-white dark:bg-gray-700 focus:ring-primary focus:border-primary">
    <option value="25">25 items</option>
    <option value="50">50 items</option>
    <option value="100" selected>100 items (Default)</option>
    <option value="250">250 items</option>
    <option value="500">500 items</option>
    <option value="1000">MAX (1000)</option>
  </select>
  <p id="limit-warning" class="hidden mt-2 text-sm text-yellow-600 dark:text-yellow-400">
    <svg class="w-4 h-4 inline mr-1" fill="currentColor" viewBox="0 0 20 20">
      <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
    </svg>
    Large lists may take longer to process and import
  </p>
</div>
```
  </action>
  <verify>
Open the list wizard in a browser:
1. Navigate to /lists and click "Create Custom List"
2. Select Movies or TV Shows to proceed to filters step
3. Verify limit dropdown shows: 25, 50, 100 (Default), 250, 500, MAX (1000)
4. Verify 100 is pre-selected
5. Select 500 or MAX - warning message should appear
6. Select 100 or lower - warning should hide

Run: `pytest tests/ -v --tb=short` to ensure no regressions.
  </verify>
  <done>
- wizardState.filters.limit defaults to 100
- Limit dropdown options updated to 25, 50, 100, 250, 500, 1000
- 100 is pre-selected and labeled "(Default)"
- Warning text appears for 500 or 1000 selection
- Legacy 10/20 limits silently migrate to 25 in edit mode
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **Model check:**
   ```bash
   python -c "from listarr.models.service_config_model import ServiceConfig; print('tmdb_region' in dir(ServiceConfig))"
   ```
   Should print True.

2. **Form check:**
   ```bash
   grep -n "tmdb_region" listarr/forms/settings_forms.py
   grep -n "tmdb_region" listarr/templates/settings.html
   ```
   Should show region field in form and template.

3. **Wizard limit check:**
   ```bash
   grep -n "1000" listarr/static/js/wizard.js
   grep -n "limit-warning" listarr/templates/list_wizard.html
   ```
   Should show MAX option and warning element.

4. **Test suite:**
   ```bash
   pytest tests/ -v --tb=short
   ```
   All tests must pass.
</verification>

<success_criteria>
- ServiceConfig model has tmdb_region column (nullable String(2))
- Settings page shows region dropdown in TMDB section
- Region value persists when saved and reloaded
- Wizard limit dropdown shows 25, 50, 100 (default), 250, 500, MAX (1000)
- Warning appears when selecting 500 or 1000
- Legacy 10/20 limits migrate to 25 in edit mode
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-list-enhancements/06.2-02-SUMMARY.md`
</output>
