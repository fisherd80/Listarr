---
status: complete
phase: 03.1-update-config-page-tags
source: [03.1-01-SUMMARY.md]
started: 2026-01-24T10:15:00Z
updated: 2026-01-24T10:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Tag Input Enabled for Radarr
expected: Navigate to /config page. Expand "Import Settings" under Radarr section. The tag text input should be enabled (not grayed out/disabled). Helper text should appear below the input.
result: pass

### 2. Tag Input Enabled for Sonarr
expected: On /config page, expand "Import Settings" under Sonarr section. The tag text input should be enabled (not grayed out/disabled). Helper text should appear below the input.
result: pass

### 3. Create Tag in Radarr
expected: Enter a new tag name (e.g., "test-listarr") in the Radarr tag input. Save Import Settings. Verify success message. Check Radarr UI to confirm the tag was created.
result: pass

### 4. Create Tag in Sonarr
expected: Enter a new tag name (e.g., "test-listarr") in the Sonarr tag input. Save Import Settings. Verify success message. Check Sonarr UI to confirm the tag was created.
result: pass

### 5. Tag Persistence After Reload
expected: After saving a tag, refresh the page. Expand Import Settings again. The previously saved tag name should appear in the input field.
result: pass

### 6. Tag Normalization (Spaces)
expected: Enter a tag with spaces (e.g., "My Test Tag"). Save. The tag should be normalized to hyphens (e.g., "my-test-tag") when saved to Radarr/Sonarr.
result: pass

### 7. Tag Normalization (Uppercase)
expected: Enter a tag with uppercase letters (e.g., "TestTag"). Save. The tag should be normalized to lowercase (e.g., "testtag") when saved.
result: pass

### 8. Clear Tag
expected: Clear the tag input (make it empty). Save Import Settings. Verify success message. Refresh page - tag field should remain empty.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
