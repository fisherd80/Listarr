---
phase: 03.1-update-config-page-tags
plan: 01
type: execute
---

<objective>
Add tag selection to the Config page Import Settings, following the established patterns for quality profiles and root folders.

Purpose: Enable users to configure a default tag for Radarr and Sonarr imports from the Config page, completing the import settings functionality that was missed during original config page creation.

Output: Working tag dropdowns in Config page Import Settings that fetch tags from Radarr/Sonarr, persist selections to MediaImportSettings, and are available as defaults for list creation wizard.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Key files (follow these patterns exactly):**
@listarr/routes/config_routes.py (lines 291-356 for quality profiles/root folders pattern)
@listarr/static/js/config.js (lines 116-248 for Radarr data fetching pattern)
@listarr/templates/config.html (lines 163-189 for dropdown pattern)
@listarr/models/service_config_model.py (MediaImportSettings has default_tag_id column)
@listarr/services/radarr_service.py (get_tags function exists at line 133)
@listarr/services/sonarr_service.py (get_tags function exists at line 133)

**Established patterns:**
- AJAX endpoints return `{"success": true, "data": [...]}` or `{"success": false, "message": "..."}`
- JavaScript fetches data on first import settings expand, not on page load
- Dropdowns show "Loading..." initially, then populate from API response
- Save handler collects all field values including new tag selection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend routes for fetching and saving tags</name>
  <files>listarr/routes/config_routes.py</files>
  <action>
Add four new routes following the existing quality profiles/root folders pattern:

1. `/config/radarr/tags` (GET) - Fetch tags from Radarr API
   - Get ServiceConfig for RADARR
   - Decrypt API key
   - Call `get_tags(base_url, api_key)` from radarr_service
   - Return `{"success": true, "tags": [{"id": N, "label": "..."}]}`
   - Handle errors same as quality profiles endpoint

2. `/config/sonarr/tags` (GET) - Fetch tags from Sonarr API
   - Same pattern as Radarr

3. Update `fetch_radarr_import_settings()` to include `default_tag_id` in response
4. Update `save_radarr_import_settings()` to accept and save `tag_id` parameter
5. Update `fetch_sonarr_import_settings()` to include `default_tag_id` in response
6. Update `save_sonarr_import_settings()` to accept and save `tag_id` parameter

Import `get_tags as get_radarr_tags` from radarr_service at top of file.
Import `get_tags as get_sonarr_tags` from sonarr_service at top of file.

Tag is optional - if not provided or empty string, set default_tag_id to None.
  </action>
  <verify>
Run: `python -c "from listarr.routes.config_routes import fetch_radarr_tags, fetch_sonarr_tags"` succeeds without import errors
Run: `pytest tests/routes/test_config_routes.py -v` - existing tests still pass
  </verify>
  <done>
- Two new GET routes for fetching tags exist
- Import settings GET responses include default_tag_id
- Import settings POST handlers accept and save tag_id
- All existing config_routes tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update frontend template and JavaScript for tag dropdowns</name>
  <files>listarr/templates/config.html, listarr/static/js/config.js</files>
  <action>
**config.html changes:**

1. Replace Radarr disabled text input (lines 224-238) with a select dropdown:
```html
<div>
  <label for="radarr-tags" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
    Tags
  </label>
  <select
    id="radarr-tags"
    name="radarr_tags"
    class="block w-full px-3 py-2 rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100 focus:ring-primary focus:border-primary"
  >
    <option value="">Loading...</option>
  </select>
</div>
```

2. Same change for Sonarr tags (lines 477-491) - replace text input with select dropdown.

**config.js changes:**

1. Add `fetchRadarrTags()` function following `fetchRadarrQualityProfiles()` pattern:
   - Fetch from `/config/radarr/tags`
   - Populate `#radarr-tags` select with options
   - First option should be `<option value="">None (no tag)</option>`

2. Add `fetchSonarrTags()` function - same pattern

3. Update `toggleImportSettings()` to also call the new tags fetch function in the Promise.all

4. Update `loadRadarrSavedSettings()` to apply saved `default_tag_id` to the dropdown

5. Update `loadSonarrSavedSettings()` to apply saved `default_tag_id` to the dropdown

6. Update save handler for Radarr (line 510-588) to include `tag_id` in the POST body:
   - Get value from `#radarr-tags` select
   - If empty string, send null or omit from body
   - Add to JSON body as `tag_id`

7. Update save handler for Sonarr (line 590-677) - same pattern
  </action>
  <verify>
Run: `python run.py` and verify:
- Navigate to /config
- Expand Radarr Import Settings - Tags dropdown shows "Loading..." then populates (if Radarr configured)
- Expand Sonarr Import Settings - Tags dropdown shows "Loading..." then populates (if Sonarr configured)
- Save settings with tag selected - no JavaScript errors in console
  </verify>
  <done>
- Tags dropdowns appear and are enabled in both Radarr and Sonarr sections
- Dropdowns populate with tags fetched from services
- "None (no tag)" option available as default
- Save functionality includes tag selection
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests for tag routes and integration</name>
  <files>tests/routes/test_config_routes.py</files>
  <action>
Add tests following the existing quality profiles/root folders test patterns:

1. `test_fetch_radarr_tags_success` - Mock radarr_service.get_tags, verify response format
2. `test_fetch_radarr_tags_not_configured` - No ServiceConfig, expect 400
3. `test_fetch_radarr_tags_api_error` - get_tags returns empty list, verify handling
4. `test_fetch_sonarr_tags_success` - Same pattern for Sonarr
5. `test_fetch_sonarr_tags_not_configured` - Same pattern
6. `test_fetch_sonarr_tags_api_error` - Same pattern
7. `test_save_radarr_import_settings_with_tag` - Include tag_id in POST, verify saved
8. `test_save_radarr_import_settings_no_tag` - Omit tag_id, verify default_tag_id is None
9. `test_fetch_radarr_import_settings_includes_tag` - Verify tag_id in GET response
10. `test_save_sonarr_import_settings_with_tag` - Same pattern
11. `test_save_sonarr_import_settings_no_tag` - Same pattern
12. `test_fetch_sonarr_import_settings_includes_tag` - Same pattern

Use existing test fixtures and mocking patterns from the file.
  </action>
  <verify>
Run: `pytest tests/routes/test_config_routes.py -v` - all tests pass including new ones
Run: `pytest tests/ -v` - full test suite passes (363+ tests)
  </verify>
  <done>
- 12 new tests added for tag functionality
- All tests pass
- Full test suite remains green
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest tests/routes/test_config_routes.py -v` passes all tests
- [ ] `pytest tests/ -v` passes full suite (363+ tests expected)
- [ ] Manual verification: Config page loads without errors
- [ ] Manual verification: Tags dropdown populates when Import Settings expanded (if service configured)
- [ ] Manual verification: Tag selection persists after save and page reload
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript/JavaScript console errors
- Tag functionality works end-to-end for both Radarr and Sonarr
- Tags available as defaults for list creation wizard (verify dropdown pre-fills if set)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-update-config-page-tags/03.1-01-SUMMARY.md`
</output>
