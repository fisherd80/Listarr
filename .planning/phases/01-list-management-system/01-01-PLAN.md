---
phase: 01-list-management-system
plan: 01
type: execute
---

<objective>
Implement list display and basic CRUD operations for TMDB list management.

Purpose: Enable users to view, create, edit, and delete TMDB lists through the web interface, establishing the foundation for all list-based automation features.

Output: Functional list management page with full CRUD operations persisting to the database.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@listarr/models/lists_model.py
@listarr/routes/lists_routes.py
@listarr/templates/lists.html
@listarr/templates/base.html

**Tech stack available:**
- Flask 3.0.0 with Jinja2 templates
- SQLAlchemy 2.0.23 ORM with SQLite
- Tailwind CSS via CDN
- WTForms for form validation and CSRF protection
- Existing List model with all fields defined

**Established patterns:**
- Blueprint-based routing in listarr/routes/
- Jinja2 templates in listarr/templates/
- Flask-WTF forms in listarr/forms/
- snake_case naming, double quotes, 4-space indentation
- Error handling with try/except and db.session.rollback()
- Flash messages for user feedback

**Constraining decisions:**
- Single-user homelab deployment (no authentication required)
- Maintain existing Flask + SQLAlchemy stack
- Follow existing codebase patterns from config/settings pages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create list display table with view all lists functionality</name>
  <files>listarr/routes/lists_routes.py, listarr/templates/lists.html</files>
  <action>
Update lists_routes.py to query all List objects from database and pass to template. In lists.html, create a Tailwind-styled table showing: name, target_service (Radarr/Sonarr), tmdb_list_type, is_active status (badge), created_at timestamp, and action buttons (Edit, Delete). Use existing base.html patterns for styling consistency. Table should be responsive with proper Tailwind classes (similar to dashboard.html card structure). Display message when no lists exist. Use db.session.query(List).all() pattern, not SQLAlchemy 2.0 select() syntax (maintain consistency with existing codebase).
  </action>
  <verify>Visit http://localhost:5000/lists and confirm table renders (empty state message if no lists). Check that table structure is present with proper column headers.</verify>
  <done>Lists page displays table with columns for name, service, type, status, date, and actions. Empty state message shows when no lists exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement create list form and route handler</name>
  <files>listarr/forms/lists_forms.py, listarr/routes/lists_routes.py, listarr/templates/lists.html</files>
  <action>
Create listarr/forms/lists_forms.py with ListForm class using Flask-WTF. Required fields: name (StringField, required, max 100 chars), target_service (SelectField with choices ["RADARR", "SONARR"], required), tmdb_list_type (SelectField with choices ["trending_movies", "popular_movies", "trending_tv", "popular_tv"], required), filters_json (HiddenField, default "{}"), is_active (BooleanField, default True). Add POST handler to lists_routes.py at /lists/create route that validates form, creates List object, commits to database with try/except and db.session.rollback() on error, flashes success message, redirects to lists page. Add "Create List" button and modal/form to lists.html using Tailwind modal pattern. Use double quotes for strings, snake_case for variables, follow existing form patterns from config_forms.py. Do NOT use inline JavaScript for modals - use hidden checkbox pattern for Tailwind modals (no Alpine.js, no vanilla JS).
  </action>
  <verify>Click "Create List" button, fill form with test data (name="Test Trending Movies", target_service="RADARR", tmdb_list_type="trending_movies"), submit. Verify list appears in table and persists after page refresh. Check that CSRF token is present in form.</verify>
  <done>Create list form validates input, saves to database with all required fields, displays success flash message, and new list appears in table immediately.</done>
</task>

<task type="auto">
  <name>Task 3: Add edit and delete functionality with route handlers</name>
  <files>listarr/routes/lists_routes.py, listarr/templates/lists.html</files>
  <action>
Add /lists/edit/<int:list_id> GET route that renders edit form (reuse ListForm), prefilled with existing List data. Add /lists/edit/<int:list_id> POST route that updates existing List object, commits with error handling, flashes success, redirects to lists page. Add /lists/delete/<int:list_id> POST route that deletes List by ID with db.session.delete(), commits with error handling, flashes success, redirects to lists page. In lists.html, make Edit button link to edit page, make Delete button submit to delete route with confirmation (use onclick="return confirm('Delete this list?')" pattern from existing codebase). Both routes should return 404 if list_id not found using List.query.get_or_404(list_id). Use db.session.commit() pattern, not session.flush(). Add proper error logging with current_app.logger.error() on exceptions.
  </action>
  <verify>
Create a test list, click Edit, modify name field, save - verify changes persist. Click Delete on a list, confirm dialog, verify list is removed from table and database. Test editing non-existent ID returns 404. Test deleting non-existent ID returns 404.
  </verify>
  <done>Edit functionality updates list fields and persists to database. Delete functionality removes list after confirmation. Both handle errors gracefully with flash messages. 404 errors for non-existent lists.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Lists page displays all lists in a table
- [ ] Create list form validates and saves new lists
- [ ] Edit list form updates existing lists
- [ ] Delete list removes list after confirmation
- [ ] All database operations use proper error handling
- [ ] Flash messages appear for success/error states
- [ ] CSRF protection enabled on all forms
- [ ] No Python errors when running application
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Lists page functional with view/create/edit/delete operations
- Database operations properly wrapped in try/except with rollback
- UI consistent with existing pages (Tailwind styling)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-list-management-system/01-01-SUMMARY.md`:

# Phase 1 Plan 1: List CRUD Operations Summary

**[One-liner describing what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `path/to/file.py` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-02-PLAN.md (Enable/disable functionality and UI verification)
</output>
