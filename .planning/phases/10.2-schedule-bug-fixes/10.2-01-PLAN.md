---
phase: 10.2-schedule-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - listarr/services/scheduler.py
  - tests/unit/services/test_scheduler.py
autonomous: true

must_haves:
  truths:
    - "Scheduled jobs skip execution when target Radarr/Sonarr service is unreachable"
    - "Scheduler logs a warning when skipping due to unreachable service"
    - "Scheduled jobs execute normally when target service is online"
    - "Missing service config causes skip with error log, not crash"
  artifacts:
    - path: "listarr/services/scheduler.py"
      provides: "Pre-flight service health check in _run_scheduled_import()"
      contains: "validate_api_key"
    - path: "tests/unit/services/test_scheduler.py"
      provides: "Tests for scheduler pre-flight health check"
      min_lines: 80
  key_links:
    - from: "listarr/services/scheduler.py"
      to: "listarr/services/arr_service.py"
      via: "validate_api_key() call"
      pattern: "validate_api_key\\("
    - from: "listarr/services/scheduler.py"
      to: "listarr/services/crypto_utils.py"
      via: "decrypt_data() for API key"
      pattern: "decrypt_data\\("
---

<objective>
Add pre-flight service health check to scheduled job execution so that scheduled imports skip gracefully when the target Radarr/Sonarr service is unreachable.

Purpose: Prevents massive error cascades when a scheduled import fires while the target service is offline. Currently, every item in the list fails individually, producing enormous error output and a failed job record.

Output: Modified `_run_scheduled_import()` with health check, plus comprehensive tests.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@listarr/services/scheduler.py
@listarr/services/arr_service.py
@listarr/services/crypto_utils.py
@listarr/models/service_config_model.py
@tests/unit/services/test_job_executor.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-flight service health check to _run_scheduled_import()</name>
  <files>listarr/services/scheduler.py</files>
  <action>
Modify `_run_scheduled_import()` in `listarr/services/scheduler.py` to validate the target service is reachable before submitting a job.

Insert the health check between the `is_active` check (line ~199) and the overlap check (line ~201). The logic:

1. Add imports at the top of the file (module level):
   - `from listarr.services.arr_service import validate_api_key`
   - `from listarr.services.crypto_utils import decrypt_data`
   (Note: `ServiceConfig` is already imported)

2. In `_run_scheduled_import()`, after the `if not list_obj.is_active` check and before the `if is_list_running(list_id)` check, add:

```python
# Pre-flight: validate target service is reachable
service_config = ServiceConfig.query.filter_by(
    service=list_obj.target_service
).first()
if not service_config or not service_config.api_key_encrypted:
    logger.error(
        f"Service {list_obj.target_service} not configured, "
        f"skipping scheduled import for list {list_id} ({list_obj.name})"
    )
    return

try:
    api_key = decrypt_data(service_config.api_key_encrypted)
    if not validate_api_key(service_config.base_url, api_key):
        logger.warning(
            f"Service {list_obj.target_service} is unreachable, "
            f"skipping scheduled import for list {list_id} ({list_obj.name})"
        )
        return
except Exception as e:
    logger.error(
        f"Error validating {list_obj.target_service} for list {list_id}: {e}"
    )
    return
```

Key design decisions:
- Use `logger.warning` (not error) for unreachable service — this is expected/transient
- Use `logger.error` for missing config — this is a configuration problem
- Catch broad Exception for decrypt/validate — scheduler must not crash
- Include list name in log messages for easier debugging
- Do NOT create a job record when service is unreachable (return before submit_job)
  </action>
  <verify>
Read scheduler.py to confirm:
1. Imports for `validate_api_key` and `decrypt_data` exist at module level
2. Pre-flight health check block exists between is_active check and overlap check
3. Three return paths: missing config, unreachable service, decrypt/validate error
  </verify>
  <done>
`_run_scheduled_import()` validates target service reachability before calling `submit_job()`. Missing config logs error and returns. Unreachable service logs warning and returns. Decrypt/validate exceptions are caught and logged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for scheduler pre-flight health check</name>
  <files>tests/unit/services/test_scheduler.py</files>
  <action>
Create `tests/unit/services/test_scheduler.py` with tests for the pre-flight health check in `_run_scheduled_import()`.

Since `_run_scheduled_import()` is a module-level function that uses the global `_app` variable, tests need to mock the module globals and database queries. Follow the existing test patterns from `test_job_executor.py`.

Test class: `TestRunScheduledImportHealthCheck`

Tests to write:

1. **test_skips_when_service_not_configured**: Mock List.query.get to return a list with target_service="RADARR". Mock ServiceConfig.query.filter_by to return None. Assert submit_job is NOT called.

2. **test_skips_when_service_api_key_missing**: Mock ServiceConfig with api_key_encrypted=None. Assert submit_job is NOT called.

3. **test_skips_when_service_unreachable**: Mock ServiceConfig with valid encrypted key. Mock decrypt_data to return "fake_key". Mock validate_api_key to return False. Assert submit_job is NOT called.

4. **test_proceeds_when_service_reachable**: Mock validate_api_key to return True. Mock is_list_running to return False. Assert submit_job IS called.

5. **test_skips_when_decrypt_fails**: Mock decrypt_data to raise ValueError. Assert submit_job is NOT called and no exception propagates.

6. **test_skips_when_validate_raises_exception**: Mock validate_api_key to raise requests.exceptions.ConnectionError. Assert submit_job is NOT called.

Implementation approach:
- Use `@patch` to mock `listarr.services.scheduler._app` with a MagicMock that provides app_context as a context manager
- Use `@patch` to mock the database query results (List.query.get, ServiceConfig.query.filter_by)
- Use `@patch` to mock `listarr.services.scheduler.validate_api_key`
- Use `@patch` to mock `listarr.services.scheduler.decrypt_data`
- Use `@patch` to mock `listarr.services.scheduler.submit_job`
- Use `@patch` to mock `listarr.services.scheduler.is_scheduler_paused` to return False
- Use `@patch` to mock `listarr.services.scheduler.is_list_running` to return False

Import `_run_scheduled_import` from `listarr.services.scheduler`.

Each test should call `_run_scheduled_import(list_id)` directly and verify the expected behavior via mock assertions.

Run `pytest tests/unit/services/test_scheduler.py -v` to verify all tests pass.
  </action>
  <verify>
Run: `pytest tests/unit/services/test_scheduler.py -v`
All 6 tests pass.
Run: `pytest tests/ -v` to ensure no regressions (all 453+ tests pass).
  </verify>
  <done>
6 tests covering all pre-flight health check paths: missing config, missing API key, unreachable service, reachable service (proceeds), decrypt failure, validate exception. All pass alongside the existing 453 tests.
  </done>
</task>

</tasks>

<verification>
1. Read `listarr/services/scheduler.py` and confirm health check exists between is_active and overlap checks
2. Run `pytest tests/unit/services/test_scheduler.py -v` — all 6 tests pass
3. Run `pytest tests/ -v` — all 453+ tests pass (no regressions)
</verification>

<success_criteria>
- `_run_scheduled_import()` calls `validate_api_key()` before `submit_job()`
- Missing service config returns early with error log
- Unreachable service returns early with warning log
- Decrypt/validate errors caught and logged, no crash
- All 6 new tests pass
- All existing 453 tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/10.2-schedule-bug-fixes/10.2-01-SUMMARY.md`
</output>
