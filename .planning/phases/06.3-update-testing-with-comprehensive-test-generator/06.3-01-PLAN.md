---
phase: 06.3-update-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/coverage-baseline.txt
  - tests/coverage-gaps.md
autonomous: true

must_haves:
  truths:
    - "Coverage analysis has been run with pytest-cov"
    - "Baseline coverage percentage is documented"
    - "Specific uncovered lines in Phase 6.2 modules are identified"
    - "Gap priority list exists for test generation plans"
  artifacts:
    - path: "tests/coverage-baseline.txt"
      provides: "Terminal coverage report with missing lines"
      contains: "TOTAL"
    - path: "tests/coverage-gaps.md"
      provides: "Prioritized gap analysis for Phase 6.2 features"
      contains: "Priority Areas"
  key_links:
    - from: "pytest-cov"
      to: "listarr/*"
      via: "coverage measurement"
      pattern: "--cov=listarr"
---

<objective>
Run pytest-cov coverage analysis to establish baseline metrics and identify specific gaps in Phase 6.2 feature coverage (top_rated presets, region filtering, limit options).

Purpose: Coverage analysis MUST come before test generation to avoid writing tests for already-covered code and to identify exactly which lines need coverage.

Output: Baseline coverage report and prioritized gap analysis document for downstream test generation plans.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-update-testing-with-comprehensive-test-generator/06.3-RESEARCH.md
@tests/conftest.py
@tests/pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pytest-cov coverage analysis</name>
  <files>tests/coverage-baseline.txt</files>
  <action>
Run comprehensive coverage analysis using pytest-cov to establish baseline:

```bash
cd "\\10.0.0.28\Storage\Scripts\Personal\Listarr - Development"
pytest --cov=listarr --cov-report=term-missing --cov-branch tests/ > tests/coverage-baseline.txt 2>&1
```

The report should show:
- Overall coverage percentage
- Per-module coverage with missing line numbers
- Branch coverage for conditional paths

After running, examine the report to identify:
1. Overall baseline coverage percentage
2. Coverage for Phase 6.2 modules:
   - listarr/services/tmdb_service.py (top_rated functions)
   - listarr/services/tmdb_cache.py (region filtering, cache keys)
   - listarr/services/import_service.py (top_rated handling)
   - listarr/routes/lists_routes.py (wizard, preview)
  </action>
  <verify>
File tests/coverage-baseline.txt exists and contains coverage data:
- Contains "TOTAL" line with percentage
- Contains line-by-line coverage data
- Shows branch coverage information
  </verify>
  <done>Coverage baseline report saved showing current metrics for all listarr modules</done>
</task>

<task type="auto">
  <name>Task 2: Analyze gaps and create prioritized gap document</name>
  <files>tests/coverage-gaps.md</files>
  <action>
Analyze the coverage-baseline.txt output and create a structured gap analysis document at tests/coverage-gaps.md.

The document should contain:

1. **Coverage Summary**
   - Overall percentage (current)
   - Target percentage (85%+ overall, 90%+ critical paths)

2. **Priority Areas** (from research findings):
   - tmdb_service.py: get_top_rated_movies(), get_top_rated_tv()
   - tmdb_cache.py: get_top_rated_movies_cached(), get_top_rated_tv_cached(), _get_tmdb_region(), region-aware cache keys
   - import_service.py: _fetch_tmdb_items() for top_rated_movies/top_rated_tv list types
   - lists_routes.py: wizard_preview() handling of top_rated presets

3. **Specific Missing Lines**
   - List uncovered line numbers per file
   - Note any uncovered branches

4. **Test Generation Priorities**
   - Group 1: tmdb_service Phase 6.2 functions (for Plan 02)
   - Group 2: tmdb_cache Phase 6.2 features (for Plan 03)

Format the document in markdown with clear sections for each priority area.
  </action>
  <verify>
File tests/coverage-gaps.md exists and contains:
- Coverage summary section
- Priority areas section with specific file references
- Specific missing lines from coverage report
- Clear groupings for test generation plans
  </verify>
  <done>Gap analysis document created with prioritized test areas for Phase 6.2 coverage</done>
</task>

</tasks>

<verification>
- [ ] pytest-cov ran successfully with no test failures
- [ ] Coverage baseline saved to tests/coverage-baseline.txt
- [ ] Gap analysis document created at tests/coverage-gaps.md
- [ ] Phase 6.2 module gaps specifically identified
- [ ] All 415+ existing tests still pass
</verification>

<success_criteria>
- Coverage baseline established with exact percentage
- Gap analysis identifies specific uncovered lines in Phase 6.2 modules
- Clear priorities set for Plans 02 and 03 test generation
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-update-testing-with-comprehensive-test-generator/06.3-01-SUMMARY.md`
</output>
