---
phase: 10-ui-ux-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - listarr/templates/macros/status.html
  - listarr/templates/macros/ui.html
  - listarr/templates/dashboard.html
  - listarr/templates/lists.html
  - listarr/templates/schedule.html
  - listarr/templates/jobs.html
autonomous: true

must_haves:
  truths:
    - "Status badges render consistently across all pages (same HTML structure, same color mapping)"
    - "Loading spinner appears identically on dashboard, jobs, and schedule pages"
    - "Empty state pattern is consistent across lists, jobs, and schedule pages"
  artifacts:
    - path: "listarr/templates/macros/status.html"
      provides: "Reusable status badge macro with parameterized status, label, size, animate"
      contains: "macro status_badge"
    - path: "listarr/templates/macros/ui.html"
      provides: "Loading spinner and empty state macros"
      contains: "macro loading_spinner"
  key_links:
    - from: "listarr/templates/dashboard.html"
      to: "listarr/templates/macros/status.html"
      via: "Jinja2 import"
      pattern: "from.*macros/status.html.*import"
    - from: "listarr/templates/jobs.html"
      to: "listarr/templates/macros/ui.html"
      via: "Jinja2 import"
      pattern: "from.*macros/ui.html.*import"
---

<objective>
Create Jinja2 macro library for reusable UI components and replace inline duplicated HTML in templates.

Purpose: Eliminate 100+ instances of duplicated status badge, loading spinner, and empty state HTML across 4 templates. Establish a single source of truth for UI components.
Output: Two macro files (status.html, ui.html) and 4 updated templates using them.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-ux-simplification/10-RESEARCH.md
@listarr/templates/base.html
@listarr/templates/dashboard.html
@listarr/templates/lists.html
@listarr/templates/schedule.html
@listarr/templates/jobs.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jinja2 macro library files</name>
  <files>listarr/templates/macros/status.html, listarr/templates/macros/ui.html</files>
  <action>
Create `listarr/templates/macros/` directory and two macro files:

**status.html** - Status badge macro:
```jinja2
{% macro status_badge(status, label=none, size='xs', animate=false) %}
```
- Status-to-color mapping dict covering: online, offline, not_configured, running, completed, failed, pending, active, inactive, scheduled, paused, manual_only
- Color scheme MUST match current inline classes exactly (use existing Tailwind classes from current templates)
- For "running" status with animate=true: include the spinning SVG loader (copy from schedule.html line 83-86)
- `label` overrides the default status|title text
- `size` defaults to 'xs' (current badge size)
- Service badge macro for radarr/sonarr:
```jinja2
{% macro service_badge(service) %}
```
- radarr: amber colors (bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200)
- sonarr: blue colors (bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-200)

**ui.html** - Loading and empty state macros:
```jinja2
{% macro loading_spinner(message='Loading...', size='h-8 w-8') %}
```
- Centered spinner SVG with message text below (match jobs.html line 85-89 pattern)
- Configurable message and size

```jinja2
{% macro empty_state(heading, description, icon_path=none, action_url=none, action_text=none) %}
```
- Centered icon, heading, description, optional action button
- Default icon_path: clipboard SVG path from jobs.html/lists.html
- Match existing empty state pattern (text-center py-12 layout)
  </action>
  <verify>
- Files exist at `listarr/templates/macros/status.html` and `listarr/templates/macros/ui.html`
- Macro files contain valid Jinja2 syntax (no import errors when templates are rendered)
- `pytest tests/ -v` passes (ensures no template rendering breaks)
  </verify>
  <done>Two macro files exist with status_badge, service_badge, loading_spinner, and empty_state macros. All macros use exact same Tailwind classes as current inline HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline status badges and UI components in templates</name>
  <files>listarr/templates/dashboard.html, listarr/templates/lists.html, listarr/templates/schedule.html, listarr/templates/jobs.html</files>
  <action>
Update 4 templates to import and use the new macros:

**dashboard.html:**
- Add at top: `{% from 'macros/status.html' import status_badge %}`
- Replace Radarr "Connected" badge (line 47-49) and Sonarr "Connected" badge (line 91-95) with: `{{ status_badge('online', label='Connected') }}`
  Note: The actual status is set dynamically by JS, so leave the span with id="radarr-status" / id="sonarr-status" BUT give it the same class structure the macro would output. The JS `updateStatusBadge()` function already manages the class names dynamically, so the template just needs the initial HTML placeholder.
  Actually: keep these as-is since JS replaces them entirely. Focus on the scheduler-paused-badge (line 144-147): replace inline classes with macro call if possible, or leave as-is since it's visibility-toggled by JS.

  Skip dashboard.html status badges since they're all JS-managed. No macro changes needed here.

**lists.html:**
- Add at top: `{% from 'macros/status.html' import status_badge, service_badge %}` and `{% from 'macros/ui.html' import empty_state %}`
- Replace Active/Inactive badges (lines 170-177) with:
  `{{ status_badge('active' if list.is_active else 'inactive', label='Active' if list.is_active else 'Inactive') }}`
  Keep the `data-status-badge` attribute by wrapping: put the macro call inside the existing span, or modify the macro to accept extra attributes. SIMPLER: just replace the class strings and keep the span with data-status-badge. Actually the JS `toggleList()` function (lists.js line 47-54) directly sets className on the badge, so keep the current span structure with data-status-badge but use the macro for the initial server-rendered classes.
  Recommendation: Leave lists.html badges as-is since JS manipulates them. Replace only the empty state.
- Replace empty state (lines 219-241) with:
  `{{ empty_state('No lists', 'Get started by creating a new list.') }}`

**schedule.html:**
- Add at top: `{% from 'macros/status.html' import service_badge %}` and `{% from 'macros/ui.html' import empty_state %}`
- Replace service badge (line 73-75) with: `{{ service_badge(list_item.target_service) }}`
- Replace empty state (lines 118-151) with:
  `{{ empty_state('No lists', 'Create a list to start scheduling automated imports.', icon_path='M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z', action_url=url_for('main.lists_page'), action_text='Create List') }}`

**jobs.html:**
- Add at top: `{% from 'macros/ui.html' import loading_spinner, empty_state %}`
- Replace loading spinner (lines 84-90) with: `{{ loading_spinner('Loading jobs...') }}`
- Replace empty state (lines 93-114) with: `{{ empty_state('No jobs found', 'Job history will appear here when lists are imported.') }}`

CRITICAL: Do NOT replace any HTML elements that have id attributes used by JavaScript (like `id="jobs-loading"`, `id="jobs-empty"`). Instead, wrap the macro output inside those divs. For example:
```jinja2
<div id="jobs-loading" class="text-center py-12">
  {{ loading_spinner('Loading jobs...') }}
</div>
```
Wait - the macro already renders the outer div. So either: (a) make the macro NOT include the outer wrapper, just the content, or (b) keep the existing wrapper divs and only replace the inner content.

Best approach: Make the loading_spinner and empty_state macros render ONLY the inner content (spinner + text, icon + heading + description), and keep the outer wrapper divs with their IDs in the templates. This preserves JS show/hide functionality.

Actually, re-reading the macros in the research: they include wrapper divs. Adjust the macros to NOT include wrapper divs - the templates keep their own wrappers with IDs. Macros just provide the content inside.
  </action>
  <verify>
- `pytest tests/ -v` all pass (templates render correctly)
- Start the dev server (`python run.py`) and visually confirm:
  - Dashboard page loads without errors
  - Lists page shows correct Active/Inactive badges
  - Schedule page shows correct service badges and empty state
  - Jobs page shows loading spinner and empty state
  </verify>
  <done>4 templates use macros for status badges, service badges, loading spinners, and empty states. No JS-managed elements broken. All tests pass.</done>
</task>

</tasks>

<verification>
1. `pytest tests/ -v` - All 453 tests pass
2. No Jinja2 template rendering errors in any page
3. Status badges render with correct colors for each status type
4. Loading spinners animate correctly
5. Empty states show correct heading, description, and optional action button
</verification>

<success_criteria>
- Macro files exist at `listarr/templates/macros/status.html` and `listarr/templates/macros/ui.html`
- At least 3 templates use imported macros instead of inline HTML
- No duplicate status badge HTML remains in templates that could use macros
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-ux-simplification/10-01-SUMMARY.md`
</output>
