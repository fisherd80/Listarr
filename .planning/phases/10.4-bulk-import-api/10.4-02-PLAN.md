---
phase: 10.4-bulk-import-api
plan: 02
type: execute
wave: 2
depends_on: ["10.4-01"]
files_modified:
  - tests/unit/test_radarr_service.py
  - tests/unit/test_sonarr_service.py
  - tests/integration/test_import_integration.py
autonomous: true

must_haves:
  truths:
    - "bulk_add_movies function has unit tests for success, error, and empty batch cases"
    - "bulk_add_series function has unit tests for success, error, and empty batch cases"
    - "Import service batch flow has integration tests for movie and series paths"
    - "All existing tests still pass after test additions"
  artifacts:
    - path: "tests/unit/test_radarr_service.py"
      provides: "Tests for bulk_add_movies"
      contains: "TestBulkAddMovies"
    - path: "tests/unit/test_sonarr_service.py"
      provides: "Tests for bulk_add_series"
      contains: "TestBulkAddSeries"
    - path: "tests/integration/test_import_integration.py"
      provides: "Tests for batch import flow"
      contains: "TestBatchImport"
  key_links:
    - from: "tests/unit/test_radarr_service.py"
      to: "listarr/services/radarr_service.py"
      via: "import bulk_add_movies"
      pattern: "from listarr.services.radarr_service import.*bulk_add_movies"
    - from: "tests/integration/test_import_integration.py"
      to: "listarr/services/import_service.py"
      via: "import _import_movies, _flush_movie_batch"
      pattern: "from listarr.services.import_service import"
---

<objective>
Add comprehensive tests for the new bulk import functions and batch-based import flow.

Purpose: Ensure bulk_add_movies, bulk_add_series, and the batch import pipeline work correctly with proper error handling, response parsing, and edge cases.

Output: Test classes covering bulk service functions and batch import integration, all passing alongside existing tests.
</objective>

<execution_context>
@C:\Users\fishe\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fishe\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.4-bulk-import-api/10.4-01-SUMMARY.md
@tests/unit/test_radarr_service.py
@tests/unit/test_sonarr_service.py
@tests/integration/test_import_integration.py
@listarr/services/radarr_service.py
@listarr/services/sonarr_service.py
@listarr/services/import_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for bulk service functions</name>
  <files>
    tests/unit/test_radarr_service.py
    tests/unit/test_sonarr_service.py
  </files>
  <action>
    **test_radarr_service.py:** Add a new test class `TestBulkAddMovies` at the end of the file. Import `bulk_add_movies` from `listarr.services.radarr_service` (add to existing import block). All tests mock `listarr.services.radarr_service.http_session`.

    Tests to add (5 tests):

    1. `test_bulk_add_movies_success` — Mock POST response with 200 status and JSON list of 3 movie objects (each with tmdbId and title). Call `bulk_add_movies("http://localhost:7878", "key", [payload1, payload2, payload3])`. Assert: response is list of 3 items, POST called once with URL ending in `/api/v3/movie/import`, json arg is the 3-item list.

    2. `test_bulk_add_movies_empty_batch` — Call with empty list `[]`. Assert: POST still called (sends empty array), response is empty list `[]`.

    3. `test_bulk_add_movies_api_error` — Mock POST response with 500 status and `response.ok = False`, `response.raise_for_status` raises HTTPError. Assert: raises Exception/HTTPError.

    4. `test_bulk_add_movies_uses_bulk_timeout` — Mock successful POST. Call bulk_add_movies. Assert: POST call used timeout matching BULK_TIMEOUT (300), NOT ADD_TIMEOUT (120). Check `mock_session.post.call_args` for timeout kwarg.

    5. `test_bulk_add_movies_sends_correct_headers` — Mock successful POST. Call bulk_add_movies. Assert: headers include `X-Api-Key` with the provided api_key.

    **test_sonarr_service.py:** Add a new test class `TestBulkAddSeries` at the end of the file. Import `bulk_add_series` from `listarr.services.sonarr_service`. All tests mock `listarr.services.sonarr_service.http_session`.

    Tests to add (4 tests):

    1. `test_bulk_add_series_success` — Mock POST response with 200 status and JSON list of 2 series objects (each with tvdbId and title). Call `bulk_add_series("http://localhost:8989", "key", [payload1, payload2])`. Assert: response is list of 2 items, POST called with URL ending in `/api/v3/series/import`.

    2. `test_bulk_add_series_empty_batch` — Call with empty list. Assert: POST called, response is empty list.

    3. `test_bulk_add_series_api_error` — Mock 500 error. Assert: raises Exception.

    4. `test_bulk_add_series_uses_bulk_timeout` — Assert timeout kwarg matches BULK_TIMEOUT (300).

    Follow existing test patterns in these files: use `@patch("listarr.services.radarr_service.http_session")` / `@patch("listarr.services.sonarr_service.http_ MagicMock for response objects, assert on call_args.
  </action>
  <verify>
    Run `pytest tests/unit/test_radarr_service.py -v --tb=short -k "TestBulkAdd"` — all 5 tests pass.
    Run `pytest tests/unit/test_sonarr_service.py -v --tb=short -k "TestBulkAdd"` — all 4 tests pass.
  </verify>
  <done>
    - TestBulkAddMovies class with 5 tests in test_radarr_service.py
    - TestBulkAddSeries class with 4 tests in test_sonarr_service.py
    - All tests verify correct endpoint, headers, timeout, payload, and error handling
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for batch import flow</name>
  <files>
    tests/integration/test_import_integration.py
  </files>
  <action>
    Add a new test class `TestBatchImportMovies` to `test_import_integration.py`. These tests verify the batch accumulation and bulk import flow in `_import_movies`.

    Import `_import_movies` and `BATCH_SIZE` from `listarr.services.import_service` (add to existing imports).

    Tests to add (6 tests):

    1. `test_batch_import_movies_uses_bulk_endpoint` — Provide 3 TMDB items (dicts with id and title). Mock: `get_existing_movie_tmdb_ids` returns empty set, `get_exclusions` returns empty set, `lookup_movie` returns valid movie_data for each, `bulk_add_movies` returns list of 3 added movies. Assert: `bulk_add_movies` called once (single batch), `add_movie` NOT called (no single-item adds), result.added has 3 items.

    2. `test_batch_import_skips_existing_before_lookup` — Provide 5 items, mock existing_ids = {item1_id, item3_id}. Assert: lookup_movie called 3 times (not 5), result.skipped has 2 items with reason "already_exists", result.added has 3 items.

    3. `test_batch_import_skips_excluded_before_lookup` — Provide 3 items, mock excluded_ids = {item2_id}. Assert: lookup_movie called 2 times, result.skipped has 1 item with reason "on_exclusion_list".

    4. `test_batch_import_handles_lookup_failure` — Provide 3 items, mock lookup_movie returns None for item2. Assert: result.failed has 1 item with reason "not_found_in_radarr", bulk_add_movies called with 2-item batch.

    5. `test_batch_import_flushes_at_batch_size` — Provide BATCH_SIZE + 10 items (e.g., 60 items if BATCH_SIZE=50). Mock all lookups succeed, bulk_add_movies returns matching items for each call. Assert: bulk_add_movies called exactly 2 times (50 + 10), total result.added equals 60.

    6. `test_batch_import_handles_bulk_failure` — Provide 3 items, mock bulk_add_movies raises Exception. Assert: all 3 items in result.failed with the exception message as reason.

    Add a test class `TestBatchImportSeries` with 2 focused tests:

    7. `test_batch_import_series_uses_bulk_endpoint` — Same as movie test #1 but for series: mock `get_existing_series_tvdb_ids`, `get_exclusions`, `get_tvdb_id_from_tmdb`, `lookup_series`, `bulk_add_series`. Assert: bulk_add_series called, add_series NOT called.

    8. `test_batch_import_series_translates_tmdb_to_tvdb` — Provide 2 items, mock `get_tvdb_id_from_tmdb` returns different TVDB IDs. Assert: lookup_series called with TVDB IDs (not TMDB IDs).

    Mock patterns to use:
    - `@patch("listarr.services.import_service.radarr_service")` for radarr mocks
    - `@patch("listarr.services.import_service.sonarr_service")` for sonarr mocks
    - `@patch("listarr.services.import_service.tmdb_service")` for TMDB mocks
    - `@patch("listarr.services.import_service.time")` to skip sleeps
    - All tests should use the `app` fixture with `app.app_context()` context manager

    Settings dict for all tests:
    ```python
    settings = {
        "root_folder": "/movies",
        "quality_profile_id": 1,
        "monitored": True,
        "search_on_add": True,
        "season_folder": True,
        "tags": [],
    }
    ```
  </action>
  <verify>
    Run `pytest tests/integration/test_import_integration.py -v --tb=short -k "TestBatchImport"` — all 8 tests pass.
    Run `pytest tests/ -v --tb=short 2>&1 | tail -5` — full test suite passes with no failures.
  </verify>
  <done>
    - TestBatchImportMovies class with 6 tests covering batch flow, pre-flight checks, flush triggers, and error handling
    - TestBatchImportSeries class with 2 tests covering series-specific bulk import and TVDB translation
    - All existing tests still pass
    - Total new tests: 17 (9 unit + 8 integration)
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/unit/test_radarr_service.py -v -k "TestBulkAdd"` — 5 tests pass
2. `pytest tests/unit/test_sonarr_service.py -v -k "TestBulkAdd"` — 4 tests pass
3. `pytest tests/integration/test_import_integration.py -v -k "TestBatchImport"` — 8 tests pass
4. `pytest tests/ -v --tb=short` — full suite passes (all 476+ existing tests plus 17 new)
5. No import errors or collection warnings
</verification>

<success_criteria>
- 17 new tests added across 3 test files
- All new tests pass
- All existing tests pass (no regressions)
- Tests cover: bulk function success/error/edge cases, batch accumulation, pre-flight skip integration, flush triggers, bulk failure handling
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-bulk-import-api/10.4-02-SUMMARY.md`
</output>
