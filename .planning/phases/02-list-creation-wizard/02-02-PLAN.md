---
phase: 02-list-creation-wizard
plan: 02
type: execute
---

<objective>
Create the wizard UI shell with stepper component and step navigation.

Purpose: Build the foundational wizard structure that all steps will use. The stepper provides visual progress (Step X of 4), and JavaScript handles navigation between steps.

Output: Wizard template with stepper, step containers, and working next/back navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-list-creation-wizard/02-CONTEXT.md
@.planning/phases/02-list-creation-wizard/02-01-SUMMARY.md

**Key source files:**
@listarr/templates/lists.html
@listarr/routes/lists_routes.py
@listarr/static/js/lists.js

**Codebase context:**
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

**From 02-CONTEXT.md:**
- Stepper with clear visual progress: "Step 1 of 4", "Step 2 of 4", etc.
- 4 steps: Type, Filters, Import Settings, Schedule
- Preset flow shows read-only filters (Step 2), custom shows editable filters
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wizard template with stepper component</name>
  <files>listarr/templates/list_wizard.html</files>
  <action>
Create new template extending base.html with:

1. **Page header**: "Create List" title with back link to /lists

2. **Stepper component** at top:
   - Horizontal progress indicator showing 4 steps
   - Step labels: "Type", "Filters", "Import Settings", "Schedule"
   - Current step highlighted (e.g., filled circle), completed steps marked, future steps muted
   - "Step X of 4" text indicator
   - Use Tailwind: flex row, circles for steps, lines connecting them

3. **Step containers**: Four divs, one per step, only one visible at a time
   - Step 1: id="step-1" (placeholder content: "Type selection goes here")
   - Step 2: id="step-2" class="hidden" (placeholder: "Filters go here")
   - Step 3: id="step-3" class="hidden" (placeholder: "Import settings go here")
   - Step 4: id="step-4" class="hidden" (placeholder: "Schedule goes here")

4. **Navigation buttons** at bottom:
   - "Back" button (hidden on step 1)
   - "Next" button (changes to "Create List" on step 4)
   - Buttons in a flex container, right-aligned

5. **Hidden form fields** to track wizard state:
   - preset (from query param)
   - service (from query param or selected in step 1)
   - current_step (default 1)

Style consistent with existing pages (white card on gray background, rounded corners, shadow).
  </action>
  <verify>Visit /lists/wizard, see stepper with 4 steps, step 1 visible with placeholder, navigation buttons present</verify>
  <done>Wizard template renders with stepper, step containers, and navigation structure</done>
</task>

<task type="auto">
  <name>Task 2: Implement JavaScript step navigation</name>
  <files>listarr/static/js/wizard.js, listarr/templates/list_wizard.html</files>
  <action>
Create wizard.js with step navigation logic:

1. **State management**:
   ```javascript
   const wizardState = {
     currentStep: 1,
     totalSteps: 4,
     preset: null,  // from URL or null for custom
     service: null, // radarr or sonarr
     // ... form data will be added in later plans
   };
   ```

2. **Navigation functions**:
   - `goToStep(stepNumber)`: Hide all steps, show target step, update stepper UI
   - `nextStep()`: Validate current step (placeholder - always true for now), increment and show next
   - `prevStep()`: Decrement and show previous
   - `updateStepperUI()`: Update stepper circles (current, completed, future states)
   - `updateNavButtons()`: Show/hide Back button, change Next text on step 4

3. **Event listeners**:
   - Next button click -> nextStep()
   - Back button click -> prevStep()
   - Initialize from URL params (set preset/service in state)

4. **Stepper UI updates**:
   - Current step: filled primary color circle
   - Completed steps: filled with checkmark
   - Future steps: gray outline
   - Connecting lines: filled for completed, gray for future

5. Update list_wizard.html to include wizard.js script tag.

Do NOT implement actual step content or validation yet - just navigation between placeholder steps.
  </action>
  <verify>Click Next to move through steps 1-4, stepper updates, Back works, button text changes on step 4</verify>
  <done>Step navigation works, stepper visually updates, Back hidden on step 1, Next becomes "Create List" on step 4</done>
</task>

<task type="auto">
  <name>Task 3: Update wizard route to render template with context</name>
  <files>listarr/routes/lists_routes.py</files>
  <action>
Update the list_wizard route to:

1. Parse query parameters:
   - preset: string (trending_movies, trending_tv, popular_movies, popular_tv, custom, or None)
   - service: string (radarr, sonarr, or None)
   - list_id: int (for edit mode - will be used in 02-05)

2. Determine wizard mode:
   - If preset in [trending_movies, popular_movies]: service = radarr, is_preset = True
   - If preset in [trending_tv, popular_tv]: service = sonarr, is_preset = True
   - If preset == "custom" or None: is_preset = False, service from param or None

3. Render list_wizard.html with context:
   ```python
   return render_template("list_wizard.html",
       preset=preset,
       service=service,
       is_preset=is_preset,
       list_id=None,  # for edit mode later
   )
   ```

4. Pass preset and service to template for JavaScript initialization.
  </action>
  <verify>Visit /lists/wizard?preset=trending_movies&service=radarr, verify template renders with correct context</verify>
  <done>Wizard route parses params, determines mode, passes context to template</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Wizard template renders at /lists/wizard
- [ ] Stepper shows 4 steps with labels
- [ ] Step 1 visible by default, others hidden
- [ ] Next button advances through steps
- [ ] Stepper UI updates to show current/completed/future
- [ ] Back button appears on step 2+, hidden on step 1
- [ ] Next becomes "Create List" on step 4
- [ ] URL params (preset, service) passed to template
- [ ] No JavaScript errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Wizard shell ready to receive step content in 02-03, 02-04, 02-05
</success_criteria>

<output>
After completion, create `.planning/phases/02-list-creation-wizard/02-02-SUMMARY.md`
</output>
