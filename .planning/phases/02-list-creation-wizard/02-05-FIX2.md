---
phase: 02-list-creation-wizard
plan: 05-FIX2
type: fix
---

<objective>
Fix UAT-007: Restore original edit form for editing lists.

Purpose: User prefers the simpler edit_list.html form over the wizard for editing existing lists. The wizard is appropriate for creating new lists, but editing should use the dedicated edit form.

Output: Edit button on /lists returns to using edit_list route instead of wizard.
</objective>

<context>
@.planning/phases/02-list-creation-wizard/02-05-ISSUES.md

**Current state:**
- Edit button links to `/lists/wizard?list_id={id}`
- User prefers original `/lists/edit/{id}` route

**Files to modify:**
@listarr/templates/lists.html (line 148)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore edit_list route in lists.html</name>
  <files>listarr/templates/lists.html</files>
  <action>
Change line 148 from:
```html
href="{{ url_for('main.list_wizard', list_id=list.id) }}"
```

To:
```html
href="{{ url_for('main.edit_list', list_id=list.id) }}"
```

This restores the original edit form behavior.
  </action>
  <verify>Visit /lists, click Edit on a list → should open edit_list.html form, NOT wizard</verify>
  <done>Edit button uses original edit_list route</done>
</task>

</tasks>

<verification>
- [ ] Click Edit on existing list → opens edit_list.html (simple form)
- [ ] Edit form loads with correct values populated
- [ ] Save changes works correctly
- [ ] Wizard still available for creating NEW lists via preset cards
</verification>

<success_criteria>
- UAT-007 resolved
- Edit uses original form, wizard for creation only
</success_criteria>

<output>
After completion, update 02-05-ISSUES.md to move UAT-007 to Resolved section.
</output>
