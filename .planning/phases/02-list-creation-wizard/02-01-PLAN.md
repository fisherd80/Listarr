---
phase: 02-list-creation-wizard
plan: 01
type: execute
---

<objective>
Add preset cards section above the lists table for quick list creation entry points.

Purpose: Provide visual, card-based entry to the list creation wizard. Presets (Trending Movies, Trending TV, Popular Movies, Popular TV) and a "Create Custom List" card give users immediate access to list creation.

Output: Lists page with card section above table, cards styled with icon + label, click handlers ready for wizard navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-list-creation-wizard/02-CONTEXT.md
@.planning/phases/01-list-management-system/01-01-SUMMARY.md
@.planning/phases/01-list-management-system/01-02-SUMMARY.md

**Key source files:**
@listarr/templates/lists.html
@listarr/routes/lists_routes.py
@listarr/static/js/lists.js

**Codebase context:**
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

**Tech stack available:** Flask 3.0, Jinja2, Tailwind CSS, vanilla JavaScript
**Established patterns:** Hidden checkbox modal, AJAX with CSRF, Tailwind card styling (see config.html for reference)

**From 02-CONTEXT.md:**
- Card layout: Preset cards in a row, "Create Custom List" card with same style but "+" icon
- Cards are icon + label only (no poster previews)
- Clicking card opens wizard (wizard built in 02-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preset cards section to lists template</name>
  <files>listarr/templates/lists.html</files>
  <action>
Add a cards section between the page header and the lists table. Structure:

1. Section container with responsive grid (4 columns on desktop, 2 on tablet, 1 on mobile)
2. Four preset cards:
   - Trending Movies (movie icon, links to wizard with preset=trending_movies&service=radarr)
   - Trending TV (tv icon, links to wizard with preset=trending_tv&service=sonarr)
   - Popular Movies (movie icon, links to wizard with preset=popular_movies&service=radarr)
   - Popular TV (tv icon, links to wizard with preset=popular_tv&service=sonarr)
3. One "Create Custom List" card with "+" icon

Each card should be an anchor tag styled as a card with:
- White background (dark:gray-800)
- Rounded corners, shadow
- Hover effect (slight scale or shadow change)
- Icon centered above label
- Label text below icon

Use Tailwind classes consistent with existing design. Icons can be inline SVG (heroicons style) or simple text emoji initially.

Remove the "Create List" button from header (replaced by cards).
Remove the create modal entirely (wizard will replace it).
  </action>
  <verify>Run dev server, visit /lists, see 5 cards above the table. Cards have hover effects.</verify>
  <done>Cards section visible with 4 presets + 1 custom card, responsive grid layout, hover states work</done>
</task>

<task type="auto">
  <name>Task 2: Add card click handlers and wizard route placeholder</name>
  <files>listarr/routes/lists_routes.py, listarr/static/js/lists.js</files>
  <action>
1. In lists_routes.py, add a placeholder wizard route:
   ```python
   @bp.route("/lists/wizard")
   def list_wizard():
       preset = request.args.get("preset")
       service = request.args.get("service")
       # Placeholder - will be expanded in 02-02
       return f"Wizard placeholder: preset={preset}, service={service}"
   ```

2. Update lists.html card links to use url_for('main.list_wizard') with appropriate query params:
   - Preset cards: ?preset=trending_movies&service=radarr (etc.)
   - Custom card: ?preset=custom (no service yet - user chooses in wizard)

3. In lists.js, no changes needed - cards are simple links for now.

Do NOT create the wizard template yet - that's 02-02. This task just ensures clicking cards navigates correctly.
  </action>
  <verify>Click each card, verify URL changes to /lists/wizard with correct query params</verify>
  <done>All 5 cards link to wizard route with correct preset/service params, placeholder response confirms params received</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Cards section appears above lists table
- [ ] 4 preset cards with correct icons and labels
- [ ] 1 custom list card with "+" icon
- [ ] Responsive grid (4 cols desktop, 2 tablet, 1 mobile)
- [ ] All cards link to /lists/wizard with correct params
- [ ] Old create modal removed from template
- [ ] No JavaScript errors in console
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Cards visually consistent with existing Tailwind design
- Ready for 02-02 (wizard shell)
</success_criteria>

<output>
After completion, create `.planning/phases/02-list-creation-wizard/02-01-SUMMARY.md`
</output>
